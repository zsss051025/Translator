# ---- 构建产物 ----
build/
Debug/
Release/
.vs/
.vscode/
*.exe
*.dll
*.obj
*.pdb
CMakeFiles/
CMakeCache.txt
whisper_trt_env/

# Visual Studio 的 CMake 集成输出目录（CMakeSettings.json 生成）
# 注：这个目录本来就被上面的 build/ 规则匹配到（无前导斜杠的模式匹配任意层级），
# 这里显式写一条，是为了把意图说清楚，也防止以后有人直接往 out/ 根下放文件。
out/

# ---- 运行时数据 ----
# 每次运行都会生成的会话库，属于用户数据，不进仓库
*.db

# ---- 运行产物：交付物目录 ----
#
# 【为什么这里必须有一条】`--out` 的**默认值**是 `deliverables`，
# 也就是任何人正常跑一场会议都会生成 `deliverables\session-N\`，
# 里面有**真实的会议转录**（md/html/csv/srt）。
# 漏了这条的后果不是"多几个文件"，而是 `git add -A` 会把真实会议内容提交进仓库。
#
# 之前只忽略了 v_out/（我自己验证用的目录），默认目录反而没管 —— 本末倒置：
# 我自己造的目录不会跑掉，**用户的目录才会**。
deliverables/

# 验证时用的临时输出目录，统一用 out_* 命名（out_cloud / out_ab / …）
out_*/
v_out/
v.db
