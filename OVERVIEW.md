# 功能总览 · 创新点 · 大模型调用框架

> **这份文档是给"要讲清楚这个项目"用的**（方案 / 答辩 / 提交材料）。
> 与另外三份的分工：
>
> | 文档 | 用途 |
> |---|---|
> | `STATE.md` | 现在做到哪了、下一步干什么（滚动） |
> | `PROJECT.md` | 稳定规格与设计决定（为什么这么做） |
> | `PRODUCT_LOOP.md` | 闭环审查 + 结构性漏洞（这东西成不成） |
> | **`OVERVIEW.md`（本文件）** | **功能清单 / 创新点 / 架构图 —— 对外讲的时候用这份** |
>
> ⚠️ **标注约定**：✅ 已实现且验证过 · ⚠️ 已实现但**未验证到行为级** · 🚧 规划中未实现。
> **对外讲的时候不要把 🚧 说成 ✅** —— 被追问一次就全盘失信。

---

## 一、一句话

**一个能自己"听懂、记住、并越用越懂你"的桌面会议助手。**

它不只是转录和翻译 —— 它在每场会结束时**主动问你几个它确实不懂的问题**，
把你教的东西存进长期记忆，然后让下一场会的识别、翻译、纪要**都用上它**。

```
会议 → 理解 → 发现未知 → 主动询问 → 你确认 → 知识入库 → 下一次会议受益 → 继续发现
                                                    ↑___________________________|
```

---

## 二、完整功能清单

### 2.1 实时理解 ✅

| 功能 | 说明 |
|---|---|
| **双路音频采集** ✅ | WASAPI 系统环回（对方/视频）+ 麦克风（你和房间）。线上会议戴耳机时，环回收对方、麦克风收你 |
| **本地语音识别** ✅ | Whisper large-v3 + CUDA，16kHz 单声道，3 秒一段滚动推理 |
| **本地翻译** ✅ | HY-MT1.5-1.8B（llama.cpp CUDA）—— 离线、免费、数据不出本机 |
| **云端翻译（可选）** ⚠️ | `--translator cloud` 走 DeepSeek。**开发机无 Key，未验** |
| **悬浮字幕窗** ✅ | 置顶、半透明、可拖动、可滚轮回看历史 |
| **全局热键** ✅ | `Ctrl+Alt+Q` 结束（看全屏视频时控制台没焦点也收得到） |
| **语言策略** ✅ | 首次检测后**锁定**，不再每段重检（实测 auto 在 3 秒碎片上常常只有 20% 把握，判错一次整段就废） |
| **源==目标直通** ✅ | 中文会议不重复翻译，但仍出纪要（守卫实测 6/6 通过） |

### 2.2 四份交付物 ✅

| 交付物 | 说明 |
|---|---|
| 会议纪要 Markdown | 概述 / 要点 / 行动项 / 术语表，含每条结论的出处 |
| 行动项 CSV | 负责人 / 截止 / 状态，带**可信度校验**（假条目不许落盘） |
| 单文件网页 | 自包含 HTML，可直接发给人看 |
| 双语字幕 SRT | 带相对时间轴 |

### 2.3 长期记忆 · 闭环 ✅

**这是这个项目的核心，也是它区别于"转录工具"的地方。**

| 环节 | 实现 |
|---|---|
| **自动发现** ✅ | 会话结束从转录里抽专名候选。**纯规则，不碰模型** —— 让模型抽实体成本高且不稳定，而规则版能进离线自检、秒级可验 |
| **该不该问** ✅ | 三层分诊：① 本地通用词表（离线）② 判断缓存 🚧 ③ **云端 DeepSeek 判决** |
| **值得问什么** ✅ | 七条规则：值变化 / 写法冲突 / 译法不一致 / 高频未确认 / 问含义 / 本场新见 / 低置信度 |
| **怎么问** ✅ | **独立交互层**：按知识类型生成不同问法，禁止内部状态外泄（hits / confidence / 状态名一个字都不许出现） |
| **一问两用** ✅ | 「它是重要概念吗？**如果是，它指什么？**」—— 一次回答同时完成"确认"和"学含义" |
| **引子** ✅ | 模型猜一个含义 → 问题变成「我猜它的意思是「X」。对吗？」—— 把开放式问题降到按一个键 |
| **两级可信度** ✅ | `candidate`（模型猜的）/ `confirmed`（你认可的），**只有 confirmed 能影响行为** |
| **含义入库** ✅ | `knowledge.definition` —— 从"记住写法"到"**懂含义**" |
| **纠错与审计** ✅ | 每次变化写 `knowledge_history`（只追加），任何一条知识都能回答"你凭什么这么说" |

### 2.4 三腿复用 —— 学的知识真的被用上 ✅

| 腿 | 出口 | 验证级别 |
|---|---|---|
| ① **识别提示** | `initial_prompt` → Whisper 偏向这些专名 | ⚠️ **只验到"内容正确"，没验到"识别变准"** |
| ② **翻译约束** | 术语表 → 翻译模型统一译名，**按段过滤** | ✅ **行为级 A/B**（真模型、真句子） |
| ③ **摘要背景** | 含义 → 摘要 prompt（模型知道 CO-RE 是什么，不是错别字） | ⚠️ 只验到"进了 prompt" |

> **③ 是最能体现"懂了"的一条**：第一场你告诉它 CO-RE 指什么，
> 第二场纪要里它就不会把 CO-RE 写成错别字、也不会解释错。

### 2.5 Agent 工具层 ✅ / 任务环 🚧

| | 状态 |
|---|---|
| Tool Registry + 5 个只读工具（`search_knowledge` / `knowledge_history` / `list_sessions` / `get_session` / `get_session_report`） | ✅ |
| 最小规划循环（多步规划 / 预算 / `--audit` 逐步打印 / 失败说"我做不到"而不是编报告） | 🚧 |
| `--ask` 检索、写工具 + 闸、Action 跨会话追踪、`--report` 派活入口、Evidence | 🚧 |
| **知识侧永不给 agent 写权限**（它只能提出候选，不能自己确认） | 设计已定 ✅ |

### 2.6 桌面产品化 🚧

| | 状态 |
|---|---|
| 输入 API Key 就能用 / 选新建助手或用已保存的助手 / 给助手命名 / UI 里看纪要和任务清单 | 🚧 方案已定 |
| WebView2 壳 + 各页面 + key 存储 + 按助手分目录 | 🚧 |

### 2.7 验证与诊断体系 ✅（容易被低估，但它是"敢信"的来源）

| 工具 | 作用 |
|---|---|
| `--selftest` | **37 组**离线逻辑自检，秒级，不需要模型 |
| `--terms` | **秒级看见"库里的东西有没有真的生效"**（三个出口逐条打印） |
| `--gaps` / `--extract` | 该问什么 / 抽出什么，都走**同一条真实代码路径** |
| `--tools` | 看 agent 拿到的工具定义，或手动跑一个工具 |
| `tools/verify_memory.py` | **独立实现**（Python sqlite3）验数据层，与 C++ 两套构建互证 |
| `tools/knowledge_audit.py` | 库的卫生审计，**只降级不删除**（脚本里没有 delete 分支） |
| `tools/loop_evidence.py` | **把多场会话按顺序摆出来** —— "第一次不知道 → 第二次知道"的唯一证据出口 |
| `tools/ab_translation_constraint.py` | 翻译腿的**行为级**回归（真模型，空库/candidate/confirmed 三组） |
| `tools/extract_case_probe.py` | 抽取判据的取证工具（规则是**量出来的**，不是想出来的） |

---

## 三、大模型调用框架图

### 3.1 全局：哪里调了模型、走本地还是云端

```
                          ┌───────────── 本机（数据不出机器）─────────────┐
会议 / 视频音频 ──► WASAPI 环回 + 麦克风 ──► 16kHz 单声道 f32 ──┐        │
                                                               │        │
   ┌───────────────────────────────────────────────────────────┘        │
   │                                                                    │
   │   ┌───────────────────────────────┐                                │
   └──►│ ① Whisper large-v3（本地 GPU）│  ◄── initial_prompt（≤224tok）  │
       │    语音识别                    │       ↑ 只有 confirmed 的**写法** │
       └───────────────┬───────────────┘       │                        │
                       │ 原文 + 置信度 + 语言    │                        │
                       ▼                        │                        │
       ┌───────────────────────────────┐        │                        │
       │ TermFixer 纠错（纯规则）        │ ◄──────┘                        │
       └───────────────┬───────────────┘                                 │
                       ▼                                                 │
       ┌───────────────────────────────┐   ┌──────────────────────┐      │
       │ ② 翻译（本地混元 1.8B）        │   │ ②' DeepSeek（可选）   │      │
       │    llama.cpp CUDA              │◄─►│    云端逐段翻译       │      │
       └───────────────┬───────────────┘   └──────────────────────┘      │
                       │ ◄── 术语约束（**按段过滤**：只带本段真出现过的）  │
                       ▼                                                 │
              悬浮字幕窗 + 落库 ──────────► 会话结束                    │
                       │                                                 │
                       ▼                                                 │
       ┌───────────────────────────────┐                                 │
       │ ③ 抽取器（**纯规则，无模型**）  │                                 │
       └───────────────┬───────────────┘                                 │
                       │ 候选（全部 candidate，**不许影响任何行为**）      │
                       ▼                                                 │
       ┌───────────────────────────────────────────────┐                 │
       │ ④ 候选分诊（决定"值不值得占用你的注意力"）     │                 │
       │   ① 本地通用词表 ── 离线、确定性、零延迟        │                 │
       │   ② 判断缓存     ── 🚧 规划中                  │                 │
       │   ③ DeepSeek 判决 ─────────────────────────────┼──► 云端         │
       │      只发：候选词 + 类型 + 一句证据             │   {general,     │
       │      **绝不发整场转录**                        │    guess, why}  │
       │   兜底：任何一层失败 → **默认"问"**             │                 │
       └───────────────┬───────────────────────────────┘                 │
                       │ 值得问的                                        │
                       ▼                                                 │
       ┌───────────────────────────────┐                                 │
       │ ⑤ 缺口检测（纯规则七条）        │                                 │
       └───────────────┬───────────────┘                                 │
                       ▼                                                 │
       ┌───────────────────────────────┐                                 │
       │ ⑥ 交互层（无模型，纯文案）      │                                 │
       │    类型化提问 + 引子           │                                 │
       └───────────────┬───────────────┘                                 │
                       ▼                                                 │
                  ┌─────────┐                                            │
                  │ 你确认  │◄── 唯一的人力入口，只在结束时打扰一次       │
                  └────┬────┘                                            │
                       ▼                                                 │
       ╔═══════════════════════════════════════════════╗                 │
       ║ ⑦ 红线：**只有 confirmed 才能影响行为**        ║                 │
       ║    单点守卫 usable_as_constraint()             ║                 │
       ╚═══════════┬═══════════════┬═══════════════════╝                 │
                   │               │                                     │
        ┌──────────┘               └──────────┐                          │
        │ 写法                                │ 含义                      │
        ▼                                     ▼                          │
  回到 ①（识别提示）                     ┌──────────────────────┐         │
  回到 ②（翻译约束）                     │ ⑧ DeepSeek 摘要       │◄────────┘
                                        │   （**整场转录上云**） │
                                        └──────────────────────┘
                                                  │
                                                  ▼
                                          四份交付物

   ┌──────────────────────────────────────────────────────────────┐
   │ ⑨ 任务环 🚧（规划中）：目标 → 多步 tool calls → 带出处的回答   │
   │    工具：search_knowledge / list_sessions / get_session_report │
   │    **永远不给它写知识库的权限**（只能提议，不能确认）           │
   └──────────────────────────────────────────────────────────────┘
```

### 3.2 每次模型调用的输入 / 输出 / 边界

| # | 模型 | 在哪 | 输入 | 输出 | 隐私边界 |
|---|---|---|---|---|---|
| ① | Whisper large-v3 | **本地 GPU** | 3 秒音频 + `initial_prompt` | 原文 / 置信度 / 语言 | 不出机器 |
| ② | HY-MT1.5-1.8B | **本地 GPU** | system（含按段过滤的术语）+ 段文本 | 译文 | 不出机器 |
| ②' | `deepseek-chat` | 云端（可选） | 同上 | 译文 | **逐段上云** |
| ③ | —（纯规则） | 本地 | 转录 | 候选专名 | — |
| ④ | `deepseek-chat` | 云端 | **候选词 + 类型 + 一句证据** | `{general, guess, why}` | **只发几个词，不发转录** |
| ⑤⑥ | —（纯规则 + 文案） | 本地 | 候选 + 历史 | 问题文本 | — |
| ⑦ | —（红线守卫） | 本地 | 知识条目 | 允许 / 拒绝 | — |
| ⑧ | `deepseek-chat`（或本地指令模型） | 云端 / 本地 | 摘要背景（含义）+ **整场转录** | 纪要 JSON | **整场转录上云** |
| ⑨ | `deepseek-chat` 🚧 | 云端 | 目标 + 工具 schema + 观察 | tool calls | 看工具参数 |

### 3.3 三条"闸门"（比模型本身更重要）

```
闸门一：候选 ≠ 约束
        模型抽出来的东西**一律是 candidate**，进不了识别提示 / 翻译约束 / 摘要背景。
        单点守卫：usable_as_constraint()。

闸门二：模型的判决 ≠ 知识
        分诊让模型决定"问不问"，但它**永远不写库**。
        （CandidateTriage.h 结构上不 include KnowledgeStore.h —— 拿不到写权限。）

闸门三：模型的猜测 ≠ 答案
        "我猜它的意思是「X」" 只是**问法**；你按了 y 才写进 definition。
        模型是起草人，不是决策人。
```

> **这三条闸门的方向是一致的**：让模型**提议**，让人**确认**，让规则**守门**。
> 大模型的强项是"看起来懂"，而生产系统需要的是"**说得出凭什么**"——
> 这两者之间的桥就是这三道闸门。

---

## 四、创新点

### 4.1 真差异（对外讲这几个）

| # | 创新点 | 为什么它稀缺 | 怎么证明 |
|---|---|---|---|
| **1** | **把"记忆必须区分来源"做成一条机械红线** | 主流 RAG 是"检索到就用"。这里 `candidate` / `confirmed` 两级，**只有用户认可的才能影响行为**，而且守卫是**单点**的（`usable_as_constraint()`） | 行为级 A/B：同一句话，candidate 组与空库**逐字相同**，confirmed 组才纠正译名 |
| **2** | **模型在这里是"判据"不是"知识库"** | 别人用大模型**回答**问题；这里用它回答"**这件事值不值得占用你的注意力**"。它的输出是**判决**（问/不问），不是事实 —— 所以它猜错也不会污染任何东西 | 判决与知识分离是**结构性**的（分诊层拿不到写库能力）；自检里有行为级确认 |
| **3** | **猜测作为"引子"，而不是答案** | 模型先猜一个含义 → 问题从"它指什么？"（要打一整句话）变成"我猜它的意思是「X」对吗？"（按一个键）。**猜测永不绕过确认** | 实测：`WGBH` → 「我猜它的意思是「美国波士顿的一家公共广播机构，制作PBS和NPR节目」。对吗？」→ 按 y → 入库 |
| **4** | **"跳过"和"不回答"也是信息** | 跳过也会计数并沉底 —— 用户**表过态**的东西不再问第二遍。很少有系统把"用户没理我"当成信号 | `asked_count` + 自检「问够了就不再问」 |
| **5** | **术语约束必须按段过滤** | 这是**踩出来的**：全局塞进 prompt 会让模型在没提到该词的段落里**凭空编造专名**（实测 `and wife get ready to go` → 「埃丽卡和马可准备出发了」） | A/B 对照实验确认是约束造成，修完再验 |
| **6** | **提问顺序决定你能拿到哪种答案** | 「它是重要概念吗？**如果是，它指什么？**」必须排在"高频确认"**之前** —— 排后面的话，用户按个 y 就结束了，**确认拿到了、含义永远拿不到** | 自检守顺序；实测含义入库率 |
| **7** | **否定性知识也被记住** | 库里长的不只是"TV = 电视"，还有"**TV 不值得问**"这类判决 —— 这是知识库里少见的一类内容 | 通用词表 119 条 + 分诊缓存 🚧 |
| **8** | **"复用"必须在屏幕上看得见** | 机制静默运行 = 对使用者不存在。开工时明说「我带着之前学到的 N 条知识」+ 逐条列出 | 用户三场演示暴露"复用看不见" → 已补 |

### 4.2 好工程（不是创新，但让上面那些可信）

| | 说明 |
|---|---|
| **每个功能都有"怎么验"** | 37 组离线自检 + **8 个独立验证脚本**（`verify_memory` / `knowledge_audit` / `loop_evidence` / `extract_case_probe` / `ab_translation_constraint` / `passthrough_check` / `zh_extract_cases` / `agent_tools_check`） |
| **验证级别如实标注** | 三条腿各自的验证级别不同，**不许混为一谈**（识别腿 ⚠️ / 翻译腿 ✅ / 摘要腿 ⚠️） |
| **不确定的东西也能进自检** | "调 LLM"抽成**可注入回调**（确认交互的 `LineReader`、分诊的 `Judge`），自检喂脚本化序列，云端实现只是另一个回调 |
| **诊断必须显示真实的东西** | `--terms` 打印**真正送进引擎的那串**而不是"取到了几条"；`--gaps` 走**和真跑同一条**渲染路径 |
| **可观测性优先** | 库一脏，`--terms` / `loop_evidence` 立刻现形（实测一眼看出"系统在学垃圾"） |
| **工具单一入口** | "含义只能由用户提供"（唯一入口 `set_definition`）；"术语清单只有一个装配点" —— **同一件事有多条路就会走散**，这个项目已经栽过三次 |

### 4.3 行业标准（**不要**当创新讲）

本地 ASR + 大模型翻译 · 悬浮字幕 · 四份交付物 · FTS5 全文检索 · RAG 检索 ·
"用大模型做摘要" · 规则抽取实体

> 这些是**正确答案**，不是**差异点**。答辩时把它们讲成"我们也做了"，
> 把时间留给 4.1 那八条。

---

## 五、完成度与未验证项（**必须如实说**）

### 5.1 完成度（多个口径，单一数字会骗人）

| 口径 | 数字 |
|---|---|
| 按计划步骤数 | 22 / 44 ≈ **50%** |
| 按"闭环能不能演" | 闭环八步前七步实现且真实跑通 → **≈ 85%** |
| **按赛题赛道维度（工具调用 / 自主性）** | 工具层 ✅，规划循环 🚧 → **≈ 15%** |
| 按"能不能拿去比赛" | 能跑 ✅、能证效果 ⚠️、能看界面 🚧、有文字材料 🚧 → **35–45%** |

**最危险的一项**：赛道维度那 20 分要的是"区别于问答机器人"的**定性**，
而 5.2 规划循环还没做。现在这东西是一个**很好的"翻译 + 长期记忆"产品**，
但还不是**自主智能体**。

### 5.2 已实现但**未验证到行为级**的（对外必须标 ⚠️）

| 项 | 到了哪一级 |
|---|---|
| **识别腿（`initial_prompt` 让 Whisper 更准）** | ❌ **完全没验** ← 整张验证阶梯最大的洞 |
| 摘要腿（模型真的用上那份含义） | ⚠️ 只验到"进了 prompt" |
| 中文抽取 | ⚠️ 只在**手写语料**上验过，真实中文 ASR 没验 |
| 云端翻译 / 麦克风双路 | ❌ 没验 |
| 云端分诊判断的**成功路径** | ✅ 已用真实 Key 验证（`WGBH` / `PodCast` 两条） |
| 判断缓存 | 🚧 未实现 |

**补洞的最短路径（成本从低到高）**：
1. 同一段音频、两个库（空 vs 有知识）的 A/B → 补识别腿
2. 同一场转录、空背景 vs 有背景的 A/B → 补摘要腿
3. 录一场中文会议 → 补中文抽取

### 5.3 已知限制（被问到要主动说）

- **中文检索只能前缀匹配**（unicode61 不切中文）
- **抽取器在英语播客上会把短语粘成假专名**（`CarsPacked` 疑似 "cars packed"）
- **`hits` 跨会话累加**，所以任何"这场听到 N 次"的说法都是假的（已改成定性描述）
- **触发表之外的肯定说法仍是这类事故的边界**（"任何不在表里的自然肯定说法会变成一条值" —— 已踩过一次，`「是的」`把两个人名覆盖掉了）
- **没有 `--fix` 纠错命令**（§6.5 要求"纠错路径必须有"，目前只有间接路径；GUI 的知识列表会补上）
