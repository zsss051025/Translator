# 产品闭环审查与实现清单

> 这份文档回答三个问题：
> **① 产品的闭环逻辑通不通？② 哪里有洞？③ 已经做完的和没做的分别是什么、怎么实现的？**
>
> **和其它两份文档的分工**
>
> | 文档 | 是什么 | 什么时候读 |
> |---|---|---|
> | `PROJECT.md` | **稳定基准**：要做什么 / 架构 / 设计决定 / 规划 / 开发流程 | 查细节、动手前读对应章节 |
> | `STATE.md` | **滚动状态**：现在到哪了、下一步干什么、怎么干活 | 每次开工先读 |
> | **本文** | **闭环审查 + 实现清单**：通不通、哪里有洞、已完成/未完成 | 想评估"这东西成不成"、要写方案/答辩时读 |
>
> 最后更新：2026-09-17

---

## 一、产品有三个环 —— 一个完整、一个刚闭上、一个完全没有

### 外环 · 一场会话（✅ 完整）

```
感知 ──→ 识别 ──→ 理解 ──→ 提取 ──→ 交付
(音频)  (Whisper)(翻译)  (纪要/行动项) (md/html/csv/srt)
                              │
                              └──→ 落库（sessions / segments）
```

用户打开就听 → 结束就给总结。**这个环是完整的，而且是用真实验音验证过的。**

### 内环 · 跨会话的知识增长（✅ 刚闭上，但**燃料不可控**）

```
        ┌──────────────────────────────────────────────┐
        │                                              │
   抽取（从转录认专名）→ 落 candidate                    │
        │                                              │
   发现变化（五条规则 → 该问什么）                        │
        │                                              │
   询问用户（最多 5 个 / 每条最多 2 次）                   │
        │                                              │
   用户确认 → 落 confirmed                              │
        │                                              │
   复用（三条腿）──→ 下一场更准 ────────────────────────┘
     ├─ 识别提示（Whisper initial_prompt）
     ├─ 翻译约束（译文里的专名统一）
     └─ 摘要背景（纪要不与已确认事实矛盾）
```

**这个环在 2.6 之前是断的** —— 断在"抽取"那一环：**没有任何代码把会话内容写进知识库**。
真跑一场 16 段、DeepSeek 摘要成功的会话之后，`knowledge` 表仍然是 0 行，
于是**一声都不问，三条腿一条都拿不到东西**。现在通了，而且有两个行为级证据
（见 §六）。

### 任务环 · 派活（❌ 完全没有 —— 赛题要的正是这个）

```
用户下任务 ──→ 理解意图 ──→ 自主规划 ──→ 调工具 ──→ 交付可验收成果
「把这周的三个会                  ├─ 查会话
  整理成给老板看的周报」            ├─ 查知识库
                                ├─ 查/改任务清单
                                ├─ 生成文档
                                └─ 不确定就反问用户
```

**没有这个环，产品是"会议记录仪"，不是"数字员工"。**
`PROJECT.md` 的 P2 里「多轮 Agent Planning」「自动工具调用」被明确列为**不做** ——
那是基于"这是个工具"的判断做的规划，赛题要求变了，这两条得提到 P0。

---

## 二、逐环节审查

| # | 环节 | 怎么实现的 | 验证到什么级别 | 洞 |
|---|---|---|---|---|
| ① | **感知** | miniaudio WASAPI 环回 + 麦克风双路（16k/mono/f32 逐样本混音）；VAD 分段 | 系统音频 ✅ 真实验音<br>麦克风 ⚠️ **端到端从未验证**（无设备） | 前 7–9 秒听不到（Whisper 加载）；静音收尾把 ~2 秒静音推给 Whisper → 幻觉段（3.4 未修） |
| ② | **识别** | whisper.cpp large-v3 CUDA（sm_89）；语言检测后锁定、每 120s 重检；`initial_prompt` 喂专名；TermFixer 识别后纠错 | ✅ 真实验音（0.8s 端到端） | **语言检测无置信度阈值 → 判错一次锁 120 秒**；分句碎片化（3.1）；**识别腿效果未做行为级验证** |
| ③ | **理解** | 本地混元 HY-MT1.5-1.8B Q4_K_M / 云端 DeepSeek；术语约束按段过滤；源==目标时 passthrough | ✅ 本地真实验音 A/B<br>⚠️ **云端翻译从未跑过** | 分句碎片化导致跨句误译（「English people」→ 英国人） |
| ④ | **提取** | 云端 DeepSeek（JSON + `response_format`）/ 本地 / 规则兜底；场景判定 → 输出层标签 | ✅ 云端真实验音（用户会话 #43/#44） | **概要和要点没有防幻觉校验**（只有行动项有）；长会议被 4000 字符截断；本地路径实际不可用（混元是翻译模型） |
| ⑤ | **记忆** | `knowledge` / `knowledge_history` / `actions` + FTS5（**触发器维护外部内容表**）；upsert 三条语义；confirmed/candidate 两级；证据溯源 | ✅ L1 + 独立 oracle（Python sqlite3） | 中文抽取刚补、**无真实数据验证**；不抽多词专名；无遗忘机制 |
| ⑥ | **发现变化** | `detect_gaps()` 五条规则（值变了 / 译法不一致 / 高频未确认 / 本场新见 / 低置信度）+ 优先级 + 同键只出一条 + 上限 | ✅ L1 11 例 + 真库（seed → 3 问） | 规则③「译法不一致」**从没在真实数据上触发过**（缺这种形状的数据） |
| ⑦ | **询问** | `run_confirmation()`：是/否词表 + 不可见字符剥离 + 新值/回退/跳过；`asked_count` + `kMaxAsks=2` 实现稀缺性 | ✅ L1 18 例 + **真交互 3 场生命周期** | **GUI 下会被跳过**（stdin 不是终端，4.7 要做）；用户答案无二次确认；无撤销入口 |
| ⑧ | **更新** | `upsert()`：新建 hits=1 / 值同累加 / **值变降级 confirmed→candidate** + 写历史；用户改写记 `user_edited` | ✅ L1 | 用户手输错字会直接进 confirmed 且 `confidence=1.0` |
| ⑨ | **复用（三条腿）** | `constraint_items()` 作为 **§6.5 红线唯一出口**；`glossary_for_text()` 按段过滤 | 翻译腿 ✅ **行为级 A/B**<br>识别腿 ⚠️ 只验到"prompt 送进引擎"<br>摘要腿 ⚠️ 只验到"背景进 user 内容" | 识别提示**没有长度上限**（Whisper 只有 224 token） |
| ⑩ | **交付** | `DeliverableWriter`：md/html/csv/srt 四件；写出边界统一做 UTF-8 净化 | ✅ L1 + 真实验音产出 | 行动项跨会话追踪未做（2.8）→ "任务清单"目前只是单场；交付物无索引 |
| ⑪ | **派活** | —— | ❌ **完全没有** | 赛题 20 分的赛道维度落在这里 |

---

## 三、八个结构性漏洞（按严重程度排序）

前四个是**产品逻辑层面的**，不是"某个功能没做"。

### 🔴 漏洞 1 · 内环的燃料是"用户肯回答问题"，而这不可控

`asked_count` 到 `kMaxAsks = 2` 就沉底 —— 这是为了"提问是稀缺资源"（§1.3）刻意做的。
但它有个没被想清楚的后果：

```
用户跳过两次 → 这条知识永远不再被问，也永远不是 confirmed
             → 永远不进三条腿 → 内环对这个用户**从未启动**
```

**一个不耐烦的用户，用一年也得不到"越用越懂你"。** 而按回车跳过是最省事的操作，
也是最可能发生的行为。

**而且产品层面完全没有暴露这件事** —— 用户不会知道"因为你不回答，所以它没变聪明"。

> 可能的出路（都还没做，也都有代价）：
> · 把确认成本降到零（UI 上一键，而不是回答文本）
> · 明确把承诺分级："你回答，它才越懂你"（诚实，但弱）
> · 让**低风险**知识可以隐式确认（如同一拼写跨会话稳定出现 N 次）——⚠️ **这会撞 §6.5 红线**，
>   必须非常小心，不能让它变成"模型猜的东西自动变约束"

### 🔴 漏洞 2 · 交付物的价值几乎不依赖内环 → 用户感受不到闭环

知识库为空时，用户也能拿到 90% 的价值（字幕 + 纪要 + 行动项 + 四件交付物）。
内环改善的是**专名一致性**这种**质量**问题，不是功能增量。

```
不确认知识：  Erica，你怎么样？马可，我过得很好。      ← 名字被音译，但纪要能用
确认知识后：  Erica，你怎么样？Marco，我过得很好。     ← 更准，但用户可能没注意
```

**缺一个"闭环可见性"**：现在没有任何地方告诉用户
"因为你确认了这 4 个名字，本场译文的一致性提升了"。知识库页（例 5）
只展示了"库里有什么"，没有展示"你的回答带来了什么"。

> 这是**产品说服力**问题，也直接影响比赛评分里的"场景价值与端到端闭环"。

### 🔴 漏洞 3 · 任务环完全没有 → 它还不是"数字员工"

见 §一。这是赛题 20 分赛道维度的全部内容，也是产品定位的分水岭。

### 🟠 漏洞 4 · 概要和要点没有防幻觉校验

`DeliverableWriter` 里**只有一个校验器 `sanitize_actions`**（保护行动项：
丢弃凭空编造的条目、清空无依据的截止日期）。而 `概述` 和 `要点` 是
**模型说什么就写什么**。

实测里已经出现过模型编内容的情况（会话 #44 `and wife get ready to go` →
「埃丽卡和马可准备出发了」）—— 那次是术语约束造成的，已修；
但**没有机制拦住下一次**。纪要里一句凭空捏造的"决定"，用户会当成真事。

### 🟠 漏洞 5 · 长会议会被摘要静默截断

`LlmSummarizer.cpp:21` → `kMaxTranscriptChars = 4000`。
超过就 `break` 并加一句"（转录过长，已截断）"塞给模型。

4000 字符对中文大约是一小时会议的一半。**后半段的讨论不进摘要**，
而且用户界面上**没有任何提示**说"你的纪要只覆盖了前半场"。

### 🟠 漏洞 6 · 识别提示没有长度上限，而 Whisper 只有 224 token

`terms_to_prompt()`（`main.cpp:180`）把词条用 `, ` 拼起来，**没有上限**；
`SpeechEngine` 直接把它塞进 `wparams.initial_prompt`。

Whisper 的 `initial_prompt` 受限于上下文的一半（224 token）。
知识累积到几百条时**会被 tokenizer 静默截断**（不报错、不警告）。
`constraint_terms()` 限 40 条，但 ASR 提示用的是"知识库词条 + `--glossary` 文件"**合并后**的列表。

> 未验证的扩展性风险：**用得越久越可能触发**，而症状是"提示莫名其妙不生效了"。

### 🟡 漏洞 7 · 用户手输的错误写法会直接进 confirmed

`interpret_answer()` 的保护只有两条：长度 ≤ 60、剥掉首尾不可见字符。
打错一个字 → 直接 `confirmed` + `confidence = 1.0` → 进三条腿 → 污染后续译文。

**没有二次确认，也没有撤销入口**（知识库页 4.9 还没做）。

### 🟡 漏洞 8 · 知识只增不减，`hits` 永不衰减

`hits` 是**累计**听到次数：一年前听过的名字和今天听到的权重完全一样。
而 `constraint_terms()` 是**按 hits 取前 40** —— 老知识会把新知识挤出去。
没有遗忘/衰减机制，`archived` 要用户手动操作。

---

## 四、已完成的工作（含**怎么实现的**）

### 第 0 阶段 · 修会话 #11 的质量问题

| 步骤 | 做了什么 | 怎么实现的 | 验证 |
|---|---|---|---|
| 0.1 | 假行动项校验器 | `DeliverableWriter::sanitize_actions()`，**纯函数**，放在云端/本地/规则三条摘要路径的**汇合点**，`write()` 内再兜一层 | L1 9 例 |
| 0.2 | 术语约束译文 | `ITranslator::glossary_constraint()` 拼进译文 system prompt（**放在接口上**，避免三个后端各写一份） | L3 A/B |

### 第 1 阶段 · 让全链路可自动化

| 步骤 | 做了什么 | 怎么实现的 | 验证 |
|---|---|---|---|
| 1.1 | `--wav` 文件输入 | `WavReader`（**自己实现**，不用上游那份有 4 个缺陷的）：支持 8/16/24/32 位整型 + 32 位浮点、按平均混单声道、线性插值重采样；**不支持就报错，绝不静默输出静音**。走与实时**完全相同**的主循环，只替换"音频从哪来" | L2（jfk.wav 端到端 20 秒） |
| 1.2 | 去掉启动选择题 | `--translator local\|cloud` + `choose_translator_backend()` 纯函数。**不看有没有 key 自动决定** —— 有 key 不代表要用云端翻译 | L1 6 例 |

### 第 2 阶段 · 知识库骨架（**这一步是产品差异化的全部**）

| 步骤 | 做了什么 | 怎么实现的（关键机制） | 验证 |
|---|---|---|---|
| 2.1 | FTS5 + 三张表 + 迁移 | `CREATE TABLE IF NOT EXISTS` 顺带就是迁移；⚠️ **它只补表、永不补列**，加列必须另走 `ALTER TABLE`；**外部内容表的索引必须由触发器维护**（手写 `INSERT INTO knowledge_fts` 等于白写），并在打开库时 `rebuild` 一次自愈历史库 | L1 + **独立 oracle**（Python 自带 sqlite3，两套构建都验） |
| 2.2 | `KnowledgeStore` | 归一化键（全角折半角 + CJK 标点表）、upsert 三条语义、`can_promote`、变化历史。**核心一条：值变了必须把 confirmed 降回 candidate** —— 那条确认是针对旧值的 | L1 |
| 2.3 | 缺口检测四条规则 | `detect_gaps()` **纯函数**（不碰库、不碰模型、不依赖时间）；优先级 + 同键只出一条 + 上限 5。`--gaps` 做成只读诊断命令 | L1 11 例 + 真库 |
| 2.4 | 结束时的确认交互 | `interpret_answer()`（是/否**词表** + 不可见字符剥离）、`apply_answer()`、`run_confirmation()`。**把"读一行"抽成 `LineReader` 回调** —— 于是问答循环能进 L1，也为 GUI 留好了接缝。`asked_count` + `kMaxAsks=2` 落地"提问稀缺" | L1 18 例 + **真交互 3 场生命周期** |
| 2.5 | 三腿复用接知识库 | `constraint_items()` 作为 **§6.5 红线的唯一出口**（刻意先取全部再过滤，让 `usable_as_constraint()` 成为唯一判定点）；`glossary_for_text()` 按段过滤术语 | L1 + **L3 行为级 A/B** |
| 2.6 | 自动抽取 + `newly_seen` | `KnowledgeExtract`：**只用规则不碰模型**（首字母大写但句首不算证据 / CamelCase / 缩写 / 引号 / 人名句式）。全部落 **candidate**。新增第五条缺口规则让抽出来的东西**真能被问** | L1 + 真数据（#43 抽出 4 个全对；jfk 抽出 0 个） |
| 2.5b | UTF-8 净化 + 修「的」被当冒号 | `utf8::sanitize()` 接在 `write_file()`（四个交付物的**唯一出口**）；真病根是 `find_first_of(u8"：:")` **按单字节比较** | L1 + L3 重新导出验证 |
| 2.6b | 中文专名抽取 | R6 姓氏+佐证（称谓/动词）、R7 机构后缀（向左扫到虚词为止）；两道假阳性闸 | L1 30 组；⚠️ **只有手写语料** |

### 累计

- **自检 30 组**，秒级，不需要模型
- **12 / 29 个计划步骤**完成
- 4 个常驻验证工具：`verify_memory.py`（独立 oracle）· `ab_translation_constraint.py`（§6.5 红线行为级）· `zh_extract_cases.py`（中文抽取回归）· `--extract` / `--gaps`（只读诊断）

---

## 五、未完成的工作

### 第 2 阶段剩余

| 步骤 | 做什么 | 为什么重要 | 依赖 | 大小 |
|---|---|---|---|---|
| 2.7 | `--ask` 检索四类问法 | `KnowledgeStore::search()` **已就绪且被自检钉住**，只差暴露成命令行/UI。**这一条直接回应"这东西是 agent 吗"的直觉 —— 能对话才像智能体** | 无 | 小–中 |
| 2.8 | Action 跨会话追踪 `todo/doing/done` | 一场会的任务在纪要里就够看了；**能回答"我上周答应的事哪些还没做"才是产品** | 无 | 中 |

### 第 3 阶段 · 遗留质量项（都会在真实使用中被用户看到）

| 步骤 | 做什么 | 为什么重要 |
|---|---|---|
| 3.1 | **分句碎片化 + 中间重叠去重** | 最影响**观感**的一条。真实验音里 `English people actually use` → 「英国人实际上在使用」、`lesson` → 「教训」，根因都是切片后独立翻译、没有上下文 |
| 3.2 | 场景判定的规则路径补齐（视频/对话） | 现在场景判定靠模型返回 `content_type`；规则兜底路径不全 |
| 3.3 | Evidence：摘要/行动项可跳回段落 | `source_session` + `source_seq` 已落库，差 UI 和链接 |
| 3.4 | 静音收尾裁掉尾部静音 | `--wav jfk.wav` 实测出现幻觉段 `Thank you.` —— **实时路径同样存在** |

### 第 4 阶段 · 桌面产品化（11 步，方案已定）

`PROJECT.md §7 第 4 阶段` 有完整方案（三个决策 + 可行性证据 + 6 条架构约束 + 产品形态举例）。
要点：**4.1 输出路径按助手分目录**（否则 `session-<id>` 会被多个助手**静默互相覆盖**）→
4.2 第一个持久化配置 + 助手注册表 → 4.3 key 存储 → 4.4 WebView2 壳 → 4.5–4.10 各页面 → 4.11 任务状态流转。

### 🆕 赛题要求的新环 · agent 化（**尚未规划**）

| 做什么 | 现状 |
|---|---|
| **工具层** | 底层能力**大部分已有**：`search()` / `list_sessions()` / `fetch_segments()` / `history()` / `DeliverableWriter` / **`run_confirmation` 的 `LineReader` 接缝**。缺 `list_actions` / `update_action`（属 2.8） |
| **规划循环** | 完全没有。需要 function calling + 步数/预算上限 + 失败处理（`cpp-httplib` + `nlohmann/json` 已 vendored） |
| **派活入口** | 完全没有。建议 UI 输入框为主、"打开就听"保留 |
| **把 P2 提到 P0** | 现在 `PROJECT.md` P2 写着「多轮 Agent Planning」「自动工具调用」**明确不做** —— 要改 |

---

## 六、验证方式总览

### 验证阶梯

| 级别 | 是什么 | 现有入口 |
|---|---|---|
| **L1** | 纯逻辑自检，秒级，不需要模型 | `--selftest`（**30 组**） |
| **L2** | 端到端可复现，零交互 | `--wav <file>`；`--export <id>` |
| **L3** | 真实模型 / 真实音频 A/B | `ab_translation_constraint.py`；`--extract` on 真会话 |
| **L4** | 真实使用场景 | 真人录音（用户做） |

### 已有工具

| 命令 | 验什么 |
|---|---|
| `Translator.exe --selftest --db t.db` | 30 组纯逻辑 |
| `python tools\verify_memory.py <db>` | **独立实现**验证数据层（Python sqlite3 ≠ 项目 vendored 的 sqlite3.c） |
| `python tools\ab_translation_constraint.py` | **§6.5 红线的行为级证据**（一次性 scratch 库，不碰用户数据） |
| `python tools\zh_extract_cases.py` | 中文抽取回归（手写语料） |
| `Translator.exe --gaps` | 该问用户什么（**只读**） |
| `Translator.exe --extract <id>` | 抽取器认出了什么（默认只读，`--apply` 才写） |

### ⚠️ 五处"做完了但没验到行为级"（如实列出）

| 未验项 | 缺什么才能验 |
|---|---|
| **识别腿真的让 Whisper 听得更准** | 一段含生僻人名的英文录音 ← **可以造，建议专门补** |
| **摘要腿真的不与背景知识矛盾** | 本机没有 instruct 模型（只有翻译模型） |
| **云端翻译**（不是摘要） | 我环境没有 API key（用户有） |
| **中文抽取在真实 ASR 输出上的准确率** | 录一场中文会议 → `--extract <id>` |
| **麦克风双路端到端** | 真实麦克风输入 |

---

## 七、一句话结论

> **外环是完整的、并且用真实音频验证过；内环刚闭上、但它的燃料是"用户肯回答问题"，
> 而这件事不可控；任务环完全没有。**
>
> 所以现在的产品是**一个做得很扎实的会议记录仪**，
> 而赛题要的是**一个能被派活的数字员工**。
> 差的那一环不在"智能"上（智能部分已经跑通且有行为级证据），
> 而在**任务理解 + 规划 + 工具调用**这一层 —— 而它的底层能力大部分已经写好了，
> 缺的是把它们**封装成工具**、加一个**规划循环**、再给一个**派活入口**。
