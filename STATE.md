# STATE · 进度与工作方式

> **这份是滚动状态，每次收工必须更新。**
>
> - `PROJECT.md` = 稳定基准（架构 / 设计决定 / 规划 / 开发流程）——不常变
> - **`STATE.md`（本文件）= 现在到哪了、该怎么干活** ——每次干完就改
>
> **给"上下文被清空后的自己"看。** 读的顺序：先读这份，再按需回 `PROJECT.md` 查细节。
>
> 最后更新：2026-09-19（**第 5 阶段 Agent 化全部收口 5.1–5.8**；
> 之后根据**用户真跑的真实音频**修了 6 个真问题：碎片化 / 提示回显 / 语言漂移 /
> 幻觉段 / 负责人写错 / 上下文；
> **下一步 = 残句合并 或 `--fix` 纠错入口 或 第 4 阶段 GUI** —— 用户要求 GUI 最后做）

> ⚠️ **用户 2026-09-19 明确收口的两件事**：
> · **云端翻译不用做了**
> · **本地翻译的质量觉得差不多了**（不要再调翻译质量）
> 所以 `--context`（上下文层）保持默认关、不再继续投入；云端那条路不必验了。

---

## 零、3 分钟恢复上下文

```
1. 读本文件 §一（进度）+ §二（工作方式）——这两节决定"接着干什么、怎么干"
2. 跑一次验证，确认环境是好的（§六）
3. 看 §一 的「下一个动作」，直接开工
```

**不要**从 `PROJECT.md` 第一章从头读 —— 它是查询用的，不是入口。

**三份文档的分工**：`STATE.md`（这份，滚动状态）· `PROJECT.md`（稳定基准 / 规格）·
**`PRODUCT_LOOP.md`（闭环审查 + 八个结构性漏洞 + 完成/未完成清单）** ——
要评估"这东西成不成"、要写方案或答辩时读第三份。

**当前阶段**：**第 5 阶段 Agent 化全部完成（5.1–5.8）**。
**下一步 = 第 4 阶段 GUI（桌面产品化）** —— 用户明确要求放在最后。

> ⚠️ **5.2–5.4 有一个必须先说清的依赖**：function calling 只有**云端 DeepSeek** 能做。
> 本地混元 `HY-MT1.5-1.8B` 是翻译模型、不会调工具（实测它做摘要会原样回显）。
> 所以规划循环的 LLM 通道走 `deepseek-chat` 的 tools 接口，
> 代价是 **agent 那条链路上云** —— 演示时要能说清"离线时退化成什么"
> （答案：退化成单次检索 `--search` + `--export`，`tools/demo.py` 第 5 幕会**当场演这个降级**）。

> ⚠️ **判断缓存（2.12）有一条明知故犯的取舍**：缓存键只有归一化后的词，**不含上下文**。
> 所以同一个词换了语境（`pilot` 普通词 → `Pilot` 产品名）会沿用第一次的判决。
> 要上下文就得每次都问模型，那缓存就没意义了 → 代价由
> `--triage-cache --forget <词>` 兜住。**答辩时主动说这条**，比被问出来好。

> ⚠️ **5.7 的核心不是"打印出处"，是"证伪出处"**：模型编的 `#9002·7`
> 和真的 `#9002·3` 在屏幕上没有区别。所以每份报告出来都跑一遍
> `evidence::verify()`，`--verify-report <文件>` 也能事后复核。
> **零引用报 exit 2 而不是 0** —— 没有出处的报告是"没做这件事"，不是"做对了"。




> ⚠️ **第 5 阶段插在第 4 阶段（GUI）之前做，这是有意的。**
> 赛题赛道维度 20 分里写明「工具调用…是区别于'问答机器人'的关键」——
> GUI 影响的是完成度观感，**Agent 影响的是定性**。文字类产出（方案大纲、答辩材料、
> 提交文档）都要靠它，而 GUI 只影响截图。
`PROJECT.md` §7 第 4 阶段有：三个决策、可行性证据、11 步顺序、6 条架构约束、
以及**产品形态举例（例 0~6）**—— 那份是需求，动 GUI 之前先读它。

---

## 一、当前进度

### 阶段总览（对应 `PROJECT.md` §7 执行顺序）

| 阶段 | 步骤 | 状态 |
|---|---|---|
| **第 0 阶段** 修会话 #11 的质量问题 | 0.1 假行动项校验器 | ✅ 完成 |
| | 0.2 术语约束翻译 | ✅ 完成 |
| **第 1 阶段** 让全链路可自动化 | 1.1 `--wav` 文件输入 | ✅ 完成 |
| | 1.2 去掉启动选择题 | ✅ 完成 |
| **第 2 阶段** 知识库骨架 | 2.1 FTS5 + 三张表 + 迁移 | ✅ 完成 |
| | **2.2 `KnowledgeStore`**（归一化 / upsert / 状态提升 / 历史） | ✅ 完成 |
| | **2.3 缺口检测四条规则** | ✅ 完成 |
| | **2.4 结束时确认交互** | ✅ 完成 |
| | **2.5 三腿复用接知识库** | ✅ 完成 |
| | **2.6 自动抽取 + `newly_seen` 规则** | ✅ 完成 |
| | **2.5b 交付物 UTF-8 净化 + 「的」被当成全角冒号（真病根）** | ✅ 完成 |
| | **2.6b 中文专名抽取（R6/R7）** | ✅ 完成（⚠️ 只在手写语料上验过） |
| | **2.6c 抽取精度回归修复 + 库清理 + `--terms` 诊断** | ✅ 完成（用户真跑 #47 暴露的） |
| | **2.6d 提问质量：写法冲突合并 / R3 全大写收紧 / 措辞改人话** | ✅ 完成（用户真跑 #13 暴露的） |
| | **2.7 含义入库（`definition` + 「它指什么」+ 进摘要背景/检索）** | ✅ 完成（**闭环的最后一环**） |
| | **2.8 独立交互层（文案与检测逻辑解耦 / 类型化提问 / 内部状态不外泄）** | ✅ 完成 |
| | **2.8b 含义进历史 + `loop_evidence.py`（闭环证据工具）** | ✅ 完成 |
| | **2.8c–2.8f 用户真跑三场演示暴露的 4 个问题**（措辞错配 / 日志淹没 / 「是的」数据损坏 / 复用不可见） | ✅ 完成（见下） |
| | **2.9–2.11 抽取中文修漏 / 分诊层（模型判"该不该问"）/ 按类型问对的问题** | ✅ 完成 |
| | **2.12 判断缓存（第 ② 层：常见词判过一次就不再联网）+ 修「同一实体被问两遍」** | ✅ 完成 |
| | 2.7 `--ask` 检索 | ✅ 完成（**做成 5.3 的退化情形**） |
| | 2.8 Action 跨会话追踪 | 未完成（**归入 5.5**） |
| **第 3 阶段** 遗留质量项 | 3.1 分句碎片化 ✅ **已做**（按停顿切）/ 3.2 场景判定 / 3.3 Evidence ✅ **已做**（=5.7）/ 3.4 静音收尾裁尾 | 3.2 / 3.4 未做；3.4 的验收标准（jfk 不再出现伪造的 `Thank you.`）**现在顺带满足了，但还没正式结项** |
| **第 4 阶段** 桌面产品化 | 4.1 输出路径+按助手分目录 / 4.2 配置+助手注册表 / 4.3 key 存储 / 4.4 WebView2 壳 / 4.5–4.10 各页面 / 4.11 任务状态流转 | **方案已定，未开工**（用户明确要求**最后做**） |
| **第 5 阶段** Agent 化（**插在第 4 阶段之前**） | 5.1 Tool Registry ✅ / 5.2 规划循环 ✅ / 5.3 `--search` 检索 ✅ / 5.4 写工具+闸 ✅ / 5.5 Action 落库 ✅ / 5.6 `--report` 派活 ✅ / 5.7 Evidence ✅ / 5.8 Demo ✅ | **全部完成** |

### 刚做完（2026-09-19，Agent 阶段收口）

| 提交 | 内容 | 一句话 |
|---|---|---|
| `faba7f4` | 5.2 / 5.6 / 5.3 agent 主干 | 规划循环 + `--report` 派活 + `--search` 检索 + `wmain`（中文参数崩溃） |
| `802f0e3` | **5.4 写工具 + 闸** | agent 只能 `propose_knowledge`（永远写 candidate），永远不能自己确认 |
| `747cef1` | 修 `loop_evidence.py` 的 ③ 自己拼 initial_prompt | 漏去重，打出 `Erika、Erika`，让好库看起来像坏库 |
| `6204866` | **2.12 判断缓存 + 修「同一实体被问两遍」** | 常见词判过就不再联网；kind 被改过之后不再重复建行 |
| `303d28c` | **5.5 行动项落库 + 跨会话聚合** | `actions` 表第一次真的被写入；台账保证幂等；状态只由用户改 |
| `bcf1cf7` | **5.7 出处可跳回段落 + 机器核对** | 引用格式唯一；`--verify-report` 当场抓编造的出处 |
| `bd25502` | **5.8 演示脚本 + 兜底** | `python tools/demo.py` 六幕演完；修掉「负责人」字段写错人的缺陷 |
| `5a9f6c9` | **识别腿 A/B：知识库默认不再进 initial_prompt** | 实测它让 Whisper **漏字/幻觉**；收益未证实 → 关掉，改走确定性的 TermFixer |
| `1780aaf` | **拦掉「自信幻觉」和纯符号段** | 真实会话 126 段里 10 段是伪造的，且**全部通过了置信度检查** |
| `4ef69f5` | **重做分句：按停顿切，不按 3 秒定时器切** | jfk 上 5 段 → 2 段，`for yourself` 的误识别跟着消失 |
| `90590cc` | **修「提示回显产出伪造内容」+ 上下文层（默认关）** | `and I'm` 译成「我是一个专业的翻译人员」——是**指令被翻译了**，前导空格是触发器 |
| `6a00c13` | **语言锁定/切换要「连续 N 段一致」** | 英文课里冒出中文原文：120 秒重检那一拍落在音乐段上 |
| 本轮 | **改成「锁定要快、切换要慢」** | 延迟锁定会让更多段走 `auto` 检测 —— 实测 jfk 106 → 79 → **8** 字符 |

### 真实音频驱动的这一轮（每条都有实测证据）

| 现象（用户真跑看到的） | 根因 | 处理 |
|---|---|---|
| 纪要"内容零散、大量识别错误" | `¶¶` 与 `Sous-titrage Société Radio-Canada` 进了交付物；**置信度 0.70~0.81，概率阈值系统性失效** | 非内容判据 + 套话黑名单（**黑名单词我自己写错过一个字母，是自检逼出来的**） |
| 跨句误译；`and I'm` → 「我是一个专业的翻译人员」 | ①"攒够 3 秒就切"造成残句 ②**system prompt 被当成待译内容翻译了一遍**（前导空格是触发器，28% 的段带） | 按停顿切；输入清洗 + 回显兜底（回退成原文） |
| 英文课里出现中文原文 | 120 秒重检那一拍落在音乐段，**一次判断就切换**了锁定语言 | 切换要连续 N 段一致；**锁定改为一段就锁** |
| 负责人写着「了，张伟」「测报告，」 | `find_owner` 是"从动词往前数 4 个汉字"，不判标点、不判是不是人名 | 公开 `looks_like_person_name()`（与抽取器 R6 共用姓氏表） |
| 「接口文档需要重写」与「…需要评审」被合成一条 | 相同片段规则只看长度，差异落在**谓语**上也照样合 | 要求相同片段"两边都落在分句结尾" |

**验证现状**：自检 44 组全过（`--selftest`，秒级、不联网、不加载模型）；
`verify_report_check` / `agent_tools_check` / `zh_extract_cases` / `verify_memory` /
`loop_evidence` 全过；`tools/demo.py` 六幕全过。

### 下一轮从这里接着走（2026-09-19 交接）

**优先级 1：残句合并**（用户最初抱怨的"被切碎的语句四不像"的根）

现状：分句已按停顿切（3.1 主体完成），但**残句仍在**，且会被翻译"补全"：
```
14  You get to get in a          → 你可以亲自体验一下。      ← 下一段才是 "car with your family…"
 7  listen as a husband and wife  → 以夫妻的身份倾听彼此的意见  ← 凭空补出"彼此的意见"
 4+5 English people actually | use → 实际上，英国人也确实如此。  ← English people 被读成"英国人"
```
设计（已想清楚，**别直接照抄，先想一下这两条**）：
· **纯函数** `looks_incomplete(text)`：以功能词结尾 / 是残句 / 无终止标点。
  ⚠️ 可复用 `tools/segment_tune.py` 里那份功能词表的**判断逻辑**，
  但**不要**把工具里那份表直接搬进来 —— 工具的表允许有噪声（只用来排序），
  产品里误判的代价是"把两句合并成一句"。
· **持有 + 超时**：`push_text` 之前拦一道。上一段"看起来没说完"就先不译；
  下一段到了：像是延续就合起来译，否则先把上一段译掉。
  ⚠️ **必须处理"等不到下一段"**：静音/会话结束时要冲刷，
  否则用户会看到**最后一句永远不翻译**。这是这个改动最容易漏的地方。
· 接入点在 `src/main.cpp` 的 `translator->push_text(result, …)`（约 6998 行），
  以及会话结束的收尾路径。

**优先级 1.5（合并黑名单 —— 这是今天新发现的，必须先做）**

⚠️ **幻觉黑名单现在有两份**：
· `src/SpeechFilter.cpp`（今天新建，21 条，带自检）
· `src/main.cpp` 约 6941 行 `blacklist`（`"谢谢观看" "订阅" "字幕" "amara" "翻译" "♪"` …）

两份都活着，改哪边都只改一半 —— 正是本项目栽过五次的那个模式。
合并时**注意 `"翻译"` 这条**：它会丢掉任何含"翻译"二字的段落，
在讨论翻译工作的会上是**明显的误杀**，要单独判断该不该留。
（这条也可能就是那份课堂笔记里某些段消失的原因，值得查一下。）

**优先级 2：`--fix` 纠错入口**（唯一算"产品缺口"的）
用户发现某条知识是错的，现在只能间接改（重抽取 / 清判断缓存）。
缺一条「改这条知识」的路 —— **一个改不了的记忆是产品风险**。

**优先级 3：第 4 阶段 GUI（4.1–4.11）** —— 4.1/4.2/4.3 已完成（助手 / 持久化配置 /
DPAPI key）；4.4 的**窗口本体已完成**（`--ui` 能开窗口，`--ui-verify` 能自动检验），
只剩"隐藏控制台"和"GUI 弹窗提问"。页面 4.5–4.11 未开工。

#### 4.4 的交接（下一次从这里接）

**已完成**
* `vcpkg install webview2:x64-windows` → `1.0.3719.77` + `wil`
  （vcpkg 是**经典模式**，和已有的 portaudio/openssl 一样）
* CMakeLists：`find_package(unofficial-webview2)` + link
  + **显式 post-build 拷贝 `WebView2Loader.dll`**（160KB，已验证落在 exe 旁边）

⚠️ **为什么 DLL 要显式拷**：vcpkg 的 applocal 是"exe 真的依赖它"时才拷，
而 WebView2Loader 是**运行时按需加载**的 —— 只要还没有代码调用 WebView2，
链接器就把依赖丢掉、post-build 也看不到它。**实测确认过**：接上 link 之后重新构建，
DLL 仍然不存在。靠"某个条件成立才生效"的部署步骤，条件不成立时就是运行时崩溃。

**窗口本体：已完成（2026-09-19，提交 `1515718` + 本条 review 修正）**

交付：`inc/WebViewWindow.h` + `src/WebViewWindow.cpp`，接在 `--ui <html>` 上。
架构见头文件注释，这里只记**两个实测出来的坑**（都会重犯，所以写死）：

1. **宿主线程必须是 STA**。三种组合都跑过：
   请求 `MTA` → **环境**创建就失败 `0x80010106 RPC_E_CHANGED_MODE`；
   请求 `STA` → 正常；不请求 → 同样失败。
   → `CoInitializeEx(COINIT_APARTMENTTHREADED)` 不是可选项。
   用 RAII 守卫（`ComApartment`）保证**所有**返回路径都 `CoUninitialize`
   —— 那个函数有 4 个出口，手写必漏一个。

2. ⚠️ **在 DSH 文件沙箱里跑，即使 STA 正确，控制器创建仍失败
   `0x8000FFFF E_UNEXPECTED`**：窗口建得出来、环境建得出来、
   `msedgewebview2` 子进程照常起、连用户数据目录都写满了，**只有最后一步失败**。
   **沙箱外同一个二进制正常**。
   → 这是**测试环境的产物，不是产品缺陷**。将来再见到 `E_UNEXPECTED`，
   先确认是不是在沙箱里跑的 —— 别再去改 STA 那段（我第一次就是这么猜错的，
   白花了一轮）。**要跑真窗口检验必须用 `danger-full-access`。**

**新增的检验手段：`--ui-verify`**
"窗口起来了"是个**看不见**的结论 —— 没有它，以后改 UI 只能靠人眼盯，
而且**"窗口白屏"和"HTML 没问题"分不出来**，正是"诊断工具撒谎"的老毛病。
现在窗口自己把渲染结果说出来：加载完 → 跑 JS 读 DOM → 打印 JSON → 自己关窗。
判据是**三者同时成立**：`probe_done` + `nav_ok` + **正文非空**。

实测（5 个用例，命令见下方"怎么检验"）：

| 用例 | 结果 |
|---|---|
| 真实页面 | `ok:true` `text_len=1237` `h1="会议纪要 · 会话 #9001"` `els=131`，exit 0 |
| **空白页（负对照）** | `ok:false` `has_text:false` **`nav_ok:true`**，exit 1 |
| 文件不存在 | `ok:false` + JSON 带 `error`，exit 1 |
| 中文目录+中文文件名 | `ok:true`，和 ASCII 路径**结果一致**（验 `to_file_url` 百分号编码） |
| 只给 `--ui-verify` 不给 `--ui` | `ok:false` + 说明怎么用，exit 1 |

空白页那条是关键：它证明这个检验量的是**内容**，不是"导航成没成功"。

**约束表仍有效，逐条标状态**

| # | 约束 | 状态 |
|---|---|---|
| 1 | **保持控制台子系统** | ⬜ 未做（`ShowWindow(SW_HIDE)` + 日志转文件） |
| 2 | **录制主循环留在 main 线程** | ✅ 窗口跑自己线程，主循环没动 |
| 3 | **`RegisterHotKey` 是线程绑定的** | ✅ 没注册任何全局热键 |
| 4 | 用户数据目录重定向到 `%LOCALAPPDATA%` | ✅ `assistant::base_dir()\webview2` |
| 5 | ✅ 部署 `WebView2Loader.dll` | ✅ 且**已验** `--ui` 能真开窗口 |
| 6 | **结束提问必须由 GUI 弹窗实现** | ⬜ **未做，且这是学习闭环在 GUI 下的断点**：现在走 `std::getline` + `if (!stdin_is_tty()) ask = false;`，GUI 里**静默跳过提问**。接缝已留好：`run_confirmation()` 收 `std::function<bool(std::string&)>` |

**可复用的现成模式**：`SubtitleWindow` 已经示范了"自己开线程 +
`GetMessageW`/`DispatchMessageW` 消息泵"，`WebViewWindow` 照它写。
**白送的**：交付物本来就是自包含单文件 HTML，WebView2 直接渲染 = 纪要查看器。

**建议的最小第一步**：`--ui` 打开一个窗口渲染一份现成的 `meeting-*.html`。
它一次验证四件事：窗口能开、WebView2 运行时能连上、HTML 能渲染、
**控制台子系统没被破坏**。之后再逐个做 4.5–4.10 的页面。
✅ **这一步已做完**，见上面"窗口本体：已完成"。

#### 怎么检验 4.4（人看 + 机器判，两条都要走）

```powershell
cd C:\dev\projects\AudioTranslator
$exe = ".\build\RelWithDebInfo\Translator.exe"
$page = "build\RelWithDebInfo\uiout\session-9001\meeting-9001.html"

# ① 人眼看：应该弹出一个窗口，里面有中文会议纪要。关掉窗口即退出。
& $exe --ui $page

# ② 机器判：应该打印一行 JSON 且 exit 0（不用管窗口，它自己关）
& $exe --ui-verify --ui $page ; echo "exit=$LASTEXITCODE"
#   期望： {"has_text":true,"nav_ok":true,"ok":true,"probe":{...},"probe_done":true}
#          text_len 是个正数（正常 1237）、h1 是"会议纪要 · 会话 #9001"

# ③ 负对照（**这条最该跑**）：证明这个检验真的会失败，而不是永远说 ok
#    ⚠️ 用 -Encoding ascii，**别用 -Encoding UTF8** —— 见 §四 的坑：
#    PowerShell 5.1 按 ANSI 读、按 UTF-8 写，中文会被双重编码。
#    这里内容纯 ASCII，用 ascii 就不可能出错。
Set-Content -Path build\RelWithDebInfo\uiout\blank.html -Encoding ascii `
  -Value '<!doctype html><html><head><meta charset="utf-8"><title>blank</title></head><body></body></html>'
& $exe --ui-verify --ui build\RelWithDebInfo\uiout\blank.html ; echo "exit=$LASTEXITCODE"
#   期望： ok:false  has_text:false  **nav_ok:true**  exit=1
#   nav_ok:true 但 ok:false = 这个检验量的是"页面上有没有字"，不是"导航成没成功"
```

⚠️ **`--ui` / `--ui-verify` 在 DSH 沙箱里会失败**（`E_UNEXPECTED`，见上面坑 2）。
在沙箱里跑要带 `danger-full-access`；你自己在普通终端里跑不受影响。

**小活（顺手可做）**：
· **3.4 结项**：验收标准是"`--wav jfk.wav` 不该再出现伪造的 `Thank you.`" ——
  **现在已满足**（改完分句后 jfk 只剩 2 段干净句子），但没正式走一遍流程结掉
· 3.2 场景判定的规则路径补齐（视频/对话）
· 方案③脚本一致性硬判据（锁定 en 时识别出汉字就丢该段）—— 语言策略的兜底
· 动作聚合的模型判断器没接（`ActionStore::ingest` 留了 `judge` 参数）
· httplib 调用点收敛（4~5 份拷贝）


| 事项 | 现状 |
|---|---|
| 识别腿：**会不会有害** | ✅ **已验**（`tools/asr_prompt_ab.py`）：喂无关词 → Whisper **漏字/幻觉**，每档重复 3 次结果一致。所以知识库默认不进提示 |
| 识别腿：**有没有用** | ❌ **仍未验**（需要一段**含易错专名**的音频；jfk 那段无提示时就基本正确，没有改进空间） |
| 摘要腿（背景进 prompt 之后纪要质量变好） | ⚠️ 只验到"进了 prompt"，没验效果 |
| 中文抽取在真实 ASR 输出上的准确率 | ⚠️ 只验了手写语料 + 种子文本 |
| 云端翻译 | ⚠️ 未做系统回归 |
| 麦克风双路 | ⚠️ 未验 |
| 中文会议里的英文词（如 `Gecko`）抽取 | ❌ 抽不出来（靠模型判断或 agent 提议补） |



### 刚做完（2026-09-17 → 09-18 一整天，共 9 个提交）

| 提交 | 内容 | 一句话 |
|---|---|---|
| `c0e9e7f` | **2.6c** 抽取精度回归 + 库清理 + `--terms` | 用户真跑 #47 暴露：8 个假阳性真进了 `initial_prompt` |
| `eb6ae40` | 自检不指定 `--db` 时不再写默认库 | 我给的命令少一个参数，静默往用户库里写假会话 |
| `c35a47e` | **2.6d** 提问质量 | 用户：「感觉根本不是人类的会问出的东西」 |
| `ea641a1` | **2.7** 含义入库（`definition`） | **闭环的最后一环**：从"记住写法"到"懂含义" |
| `5093050` | **2.8** 独立交互层 | 文案与检测逻辑解耦，内部状态不外泄 |
| `9427116` | **2.8b** 含义进历史 + `loop_evidence.py` | 让"跨会话弧线"第一次能被打印出来 |
| `be50794` | 2.8c 措辞对齐（term 说「重要概念」） | 中文 R7 产的项目名也被叫"技术术语"，错配 |
| `9acf382` | 2.8d 模型日志静音（`--verbose` 还原） | 真实启动 1483 行 → 68 行，`>>> 已开始记录 <<<` 被埋掉了 |
| `5cd344c` | **2.8e 修「是的」数据损坏** | 用户答「是的」→ 把两个人名覆盖成了「是的」 |
| `bb39a28` | **2.8f 复用可见**（开工语） | 复用一直在发生，但屏幕上一个字都没提 |

### 用户的真实演示结果（3 场，2026-09-18 01:17–01:22）

`demo.db`，三场英语学习视频（11 / 18 / 32 段）：

| | 抽出的候选 | 学到的 |
|---|---|---|
| #1（11 段） | Erica、Marco、EnglishPod、TV | TV 的含义 +（**被「是的」损坏**的两个人名） |
| #2（18 段） | **（没有）** ← 这一场空转 | 问了 EnglishPod（其实是 #1 的候选） |
| #3（32 段） | EnglishPod×4、CarsPacked、WGBH | CarsPacked 的含义 |

**判定：机制上成功，演示上只成功一半。**

- ✅ 真实音频、真实入库、`knowledge_history` 有精确流水、提问类型正确
- ⚠️ **"第二次知道"这一拍在屏幕上完全看不见** —— #1 和 #2 的 `initial_prompt` 一模一样，
  复用发生了但没人知道 → 已由 `bb39a28`（开工语）补上
- ❌ **"第三次利用历史"没有体现** —— 三场之间没有反复出现的实体
- ❌ **学到的东西和用户的工作无关**（电视节目缩写 / 英语博客 / 装车）
  → 这正是 `PRODUCT_LOOP.md` 记的那个洞："交付物价值几乎不依赖内环"
- ⚠️ **`CarsPacked` 很可能是 ASR 把 "cars packed" 粘成的假专名**，用户替它圆了个定义。
  同场的 `WGBH`（波士顿公共电台）是真的

**素材问题（代码解决不了）**：三段视频要**共用实体**（同一主题/同一集切三段），
且每场都得抽出候选（#2 那种零候选的场次直接换掉）。

**`demo.db` 现在是损坏状态**（`initial_prompt = EnglishPod, CarsPacked, 是的, TV`）。
修法（已验证可行，不用重录）：
```powershell
.\build\RelWithDebInfo\Translator.exe --extract 1 --apply --ask --db demo.db --out out_demo
# 它会自己发现值变了并问：我之前记得「Marco」是「是的」，刚才听到的是「Marco」。要更新吗？
# 答「是的」即可；Marco / Erica 两条都会恢复
```

### 下一个动作（具体到文件）

**5.2 最小规划循环**（`inc/AgentLoop.h` + `src/AgentLoop.cpp`）

- 复用已注册的 5 个只读工具（`agent::ToolRegistry`）
- **把"调 LLM"抽成可注入回调** —— 和 2.4 的 `LineReader` 同一招，
  于是规划循环能进 L1（喂脚本化的 tool call 序列，不联网）
- Agent State：消息历史 + 已调用过的工具 + 步数/预算计数
- 预算：最大步数（默认 6~8）/ 最大 token / 最大墙钟时间（默认 60s），**超了要有定义好的行为**
  （倾向：返回已完成的部分 + 明确说明为什么停，而不是报错）
- 超时 / 失败 / 取消 / 重试
- **`--audit` 逐步打印**：`→ 调用 list_sessions(days=7)` / `← 3 场会话`
  —— 这是评审判断"它真的在做事"的唯一依据，也是调试的唯一手段
- ⚠️ 失败模式必须是"**说清楚我做不到**"，绝不能编一份报告 ——
  这直接决定赛题"结果交付与可验收性"是加分还是负分

紧接着 **5.3 `--ask`**：它不是独立实现，而是 agent 的**退化情形**（单次工具调用、不规划）
—— 否则将来有两份代码。


### 其它状态

| | |
|---|---|
| 分支 | `main`（本地主线）；远程备份在 `assistant-baseline` |
| 远程 `main` | `da97d96` —— **有意不动**，详见 `PROJECT.md` §3.6 末「历史决策：不合并 da97d96」 |
| tag | `translator-final` → `7717ace`（翻译版本封存） |
| 自检 | **35 组**，秒级，不需要模型（`--selftest`） |
| 闭环八步完成度 | 八步**等权平均 88%**（听见 90 / 理解 88 / 提取 80 / **记忆 90** / **发现变化 85** / **询问 90** / **更新 90** / **再次利用 90**）<br>2.6 补上了"自动抽取"这个**原本不存在的环节**；2.5b 修掉了摘要要点标题被切坏的问题（理解 82→88、提取 78→80） |

**工作树状态**：干净（2.5b 已完成，`inc/Utf8.h` / `src/Utf8.cpp` 已入库）。

**"越用越懂你"的可验证里程碑**（第二句承诺）：

| 里程碑 | 状态 |
|---|---|
| 知识能落库、能区分 confirmed/candidate | ✅ 2.2 |
| 能发现"该问什么" | ✅ 2.3 |
| 用户答了能改进知识库 | ✅ 2.4 |
| 答过的东西真的改变识别/翻译/摘要行为 | ✅ 2.5（翻译腿已用真实失败句 A/B 证明） |
| 会答的问题不重复问 | ✅ 2.4（`asked_count` + `kMaxAsks`） |
| **能从会话里自己长出候选知识** | ✅ **2.6** ← 缺了这一步，上面五条全部拿不到输入 |
| **整条闭环在真实音频上跑通**（不靠手工插数据） | ✅ **2.6**：真实会话 #43 → 自动抽出 4 个 → 用户确认 → 译文被纠正 |

### 2.6 的端到端证据（真数据，不是编的）

```
[Extract] 会话 #43 共 16 段，开始抽取
[Extract] 抽出 4 个候选： Marco(person,2) Erica(term,2) EnglishPod(term,1) TV(term,1)
[Extract] 已写入 4 条候选（全部 status=candidate，未被用作任何约束）
[确认] 这场会话里有 4 条知识想跟你核对：
  1. 第一次听到「Marco」。这个词的写法对吗？      已确认：Marco
  2. 第一次听到「Erica」。这个词的写法对吗？      已确认：Erica
  ...
[确认] 记下 4 条，跳过 0 条

然后同一句真实失败句：
  会话 #43 当时的译文:  埃里卡，你怎么样？马可，我过得很好。
  现在:                Erica，你怎么样？Marco，我过得很好。
```

⚠️ **三条腿各自的验证级别仍然不一样**（别混为一谈）：

| 腿 | 验到了什么 | 没验到什么 |
|---|---|---|
| ① 识别提示 | prompt 字符串确实被构造并送进 `SpeechEngine`（日志 `[识别提示]`），candidate 不在里面 | **whisper 的识别结果是否真的因此变准** ← **整张阶梯最大的洞**，见下 |
| ② 翻译约束 | **行为级 A/B 通过**（`tools/ab_translation_constraint.py`） | 云端翻译路径（本机没有 API key） |
| ③ 摘要背景 | 背景确实拼进送给模型的 user 内容（自检 + 日志 `[摘要] 已注入`） | **模型是否真的用上了这个理解** —— 本机没有 instruct 模型 |

### ⚠️ 验证阶梯的两个洞（下次要补的第一件事）

| 洞 | 怎么补 | 成本 |
|---|---|---|
| **① 识别腿没有行为级验证** | **同一段音频、两个库的 A/B**：空库 vs 已有该词知识，跑 `--wav` 同一文件，比识别原文 | 低（几十分钟，可自动化） |
| **③ 摘要腿没有行为级验证** | 同一场转录、空背景 vs 有背景的 A/B，需要一个会做摘要的指令模型（本地 `--llm-model` 或云端） | 中 |

**为什么 ① 排第一**：整个验证阶梯里只有它是空的 —— 翻译腿有 `ab_translation_constraint.py` 兜着，
摘要腿至少能靠 `--terms` 看内容，而**识别腿现在只有"提示词内容对了"这种间接证据**。
而且它是"越用越准"这个核心承诺的直接证据，答辩材料里最该有的一段数据。

诚实预期：Whisper 的 `initial_prompt` 只有 224 token 预算，它"偏向"这些词的强度是**经验性的**，
所以可能出现"提示词对了但识别没变" —— **那也是有价值的结论**（说明得靠 TermFixer 那条腿兜，
或得换更强的注入方式），比一直不知道强。

---

## 二、工作方式（用户的明确要求，**别丢**）

> 这一节是用户反复纠正后才定下来的。丢掉它 = 用户会说"你和记忆丢失了一样"。

1. **每轮必须有代码动作。** 改 `.cpp/.h`，或者跑构建/测试。**纯写 md 的轮次不算一轮。**
   （历史教训：有一段时间连续十几轮只写文档，用户直接指出风格变了。）
2. **报告用固定结构**：
   ```
   # 步骤 N 完成 ✅ <产出名>
   ## 做了什么          ← 文件级表格
   ## 为什么是这一步    ← 排序理由
   ## 验证证据          ← 实测数字，不是"应该没问题"
   ```
3. **交付时说清三段**：**怎么测 / 理想结果 / 出问题怎么判断**。
4. **每个功能走完整循环**：**实现 → 验证 → git 提交 → code review → 告诉用户怎么检验**。
   code review 是**我自己审自己**，要写出发现的问题和未修的遗留，不是走过场。
5. **不要问"要不要"**。产品判断才问（清单见 `PROJECT.md` §8.9）；实现细节自己定并说明理由。
6. **证据优先，不猜。** 用实测数字说话；被证伪就当场承认并修。
   （已经发生过两次：把模型加载猜成 100 秒，实际 8.4 秒；声称修好了截止日期误杀，实际没修好。）
7. **收工前更新这份 `STATE.md`。**

---

## 三、代码风格（这个项目的写法）

### 注释

- **写"为什么"，不写"是什么"。**
- 修过的坑必须留三件套：
  ```cpp
  // 【现象】窗口永远停在"翻译中…"
  // 【原因】译文轮询被放在带 continue 的分支之后，只跑了一次
  // 【判断】若复现，先查这段轮询有没有被挪到 continue 后面
  ```
  **本项目没有测试框架，注释就是回归记忆。**

### 结构

- **纯逻辑做成 `static` 函数 / 纯函数**，这样能进 L1 自检（秒级、不需要模型）。
  已经这么做的：`strip_overlap`、`collapse_repeats`、`TermFixer`、
  `SpeechEngine::looks_like_prompt_echo` / `looks_repetitive`、
  `sanitize_actions`、`canonicalize_date`、`choose_translator_backend`、
  `WavReader::parse`、`ITranslator::glossary_constraint`、
  `knowledge::normalize_key` / `usable_as_constraint` / `fts_query_from_user_text`、
  `knowledge::detect_gaps`、`knowledge::interpret_answer`。
  > 判据：**能不能不碰数据库、不碰模型就验完？** 能，就提成纯函数。
- **交互逻辑要把"输入源"抽成回调**，别写死读 `std::cin`。
  `run_confirmation()` 收一个 `std::function<bool(std::string&)>`：
  main 注入读控制台，自检注入读 stringstream —— 于是问答循环能进 L1，
  **而且落库走的是真实路径**（不是自检构造的理想数据）。
  GUI 壳将来也从同一个接缝接进来。
- **断言要断言到机制，不能只断言"东西在"**。
  「四张表在不在」这种断言让"FTS 索引其实一条没建起来"藏了两轮。
  改成"写进去→必须查得回来→删掉→必须查不到"之后，**当场抓出两个真 bug**。
- **触发表与判定表必须分开**：抽取宁宽勿漏，判定宁严勿滥。
  （`action_markers_*` vs `strict_verbs_*` —— 混用过一次，结果一句寒暄被当成作业。）
- **接口上的共享逻辑放接口里**，别让多个后端各写一份。
  （`glossary_constraint` 就是三处 prompt 各写一份之后提取出来的。）

### 错误处理

- **不支持就报错，绝不静默给错数据。**
  （`WavReader` 遇到不支持的位深直接报错，而不是输出整段静音。）
- 错误走 `std::cerr`，正常输出走 `std::cout`，统一 `[模块名]` 前缀。
- 失败路径要给**下一步提示**，不只说"失败了"。

### 测试

- 用例的输入**必须来自真实路径**，不能自己手搓一个"更干净的"版本。
  **这条栽过两次**：`--test-window` 自己编时序；截止日期用例配了真实管线不产生的配对。
- 加自检时，**先确认这条用例在修复前会失败** —— 否则它是安慰用例。
- **别用"表存在/文件存在"当验收**。第 2 阶段已栽过一次：FTS 索引空着也能过。
  每加一层基础设施，就加一条**走真实写入路径的往返断言**。
- **断言必须与真实数据隔离**。别断言"检索某词恰好 1 条" ——
  库里真有一条同名的知识时，自检就永远红。用**本用例独有的 token**
  （`ZqFtsRoundTrip9` 那种），断言才只反映本用例干了什么。

### 提交

- **一次改动一次提交**；提交信息写**为什么**，修 bug 要带现象/原因/判断。
- 不要巨型提交（一个 36 文件的提交出问题只能整体回退，无法 `git bisect`）。
- **不要 `git add -A`** —— 先 `git status` 确认。`t.db`、`out/`、`build/` 都不该进仓库。
- 推送前先 `git fetch` 确认没分叉，**永远不要 `--force`**。

### 编码与命名

- 源码一律 **UTF-8 无 BOM**。项目用 `/utf-8` 编译，GBK 文件会刷 C4828 警告
  （`miniaudio_impl.cpp` 原来就是，已修）。**别用会写回 GBK 的编辑器。**
- 注释用中文，标识符和字符串常量用英文，**面向用户的输出用中文**。
- 常量上提为具名 `constexpr` / `static const`，不写魔法数。

---

## 四、最近的关键决定与踩坑（滚动，只留最近 10 条）

| 时间 | 事 | 关键点 |
|---|---|---|
| 09-17 | 2.6c **抽取太松：8 个假阳性真的进了 `initial_prompt`** | 用户真跑 #47（53 段播客）后确认了 8 个假专名（`down/Keep/Midnight movies/Speaking/Learners/Exactly/preview`）。**不是"库里多了几条脏数据"，是 Whisper 的识别提示被污染了** —— 下次识别会被主动往这些词上带。→ 加三条判据：强形状 / 佐证 / `cap_noninit>=2 且从未全小写出现`。真实数据上 8 个假阳性全消、4 个真名全留 |
| 09-17 | 2.6c **"用户确认过的"不等于"对的"** | 这 8 条都是用户**逐条按了确认**的。人面对"是否记住了 down？"会顺手按 y —— **把确认当成质量保证是错的**，上游该拦住的不该推给用户。这也是 §6.7 提问稀缺的另一个理由 |
| 09-17 | 2.6c **"某个键是专名"和"它出现了几次"是两件事** | 我给计数环节加了 `classify_token(t) == Ev::None` 过滤，自检立刻报「Marco 出现次数应为 2」：`Marco` 在句首那次被判成句子开头而跳过。**判定的输入是证据，计数的输入是全部出现** —— 混在一起就会给用户看错数字 |
| 09-17 | 2.6c **诊断命令行必须能看到"库里的东西有没有生效"** | §6.5 红线的可观测面就是那串 `initial_prompt`，但以前只有真跑一场会（录音 + 等 Whisper 加载上百秒）才能看到。**验一次的成本高到没人会验**，所以库被污染了没人发现，直到用户自己翻日志问"这些词哪来的"。→ 新增 `--terms`（不加载模型，几秒给同一答案） |
| 09-17 | 2.6c **同一段逻辑抄了第三遍** | 「知识库 confirmed + `--glossary` 去重合并」在主路径和 `run_dump_prompt` 里各写了一份（注释还写着"两边不可能再走散"）。本项目"诊断与真实路径走散"已踩三次，每次都是因为存在第二份拷贝 → 收敛成唯一的 `build_glossary()` |
| 09-17 | 2.6c **同 key 两种 kind 在背景里出现两行** | 条目身份是 `(kind,key)`：`Marco` 一场抽成 `person`、另一场确认成 `term`，于是摘要背景里同时有「Marco（人名）」和「Marco（术语）」。存储层没错，**下游没收敛**，对模型是自相矛盾的噪声 → `background_lines()` 按 key 去重（`constraint_terms()` 早就有，我漏了这一处） |
| 09-17 | 2.6c **测具用了真实数据的形状，就会被真实数据绊倒** | `verify_memory.py` 的 FTS 探针写的是 `("term","englishpod")` —— 库里真有了 englishpod 之后，INSERT 撞 `UNIQUE(kind,key)`，脚本自己崩，而真正要验的"触发器维护索引"根本没跑到。→ 探针必须用**不可能碰撞的合成键**（同 `__selftest_` 的思路） |
| 09-17 | 2.6c **FTS 探针不能用带下划线的词** | 换合成键后第二次假失败：`__verify_fts_a__` 被 unicode61 分词器按 `_` 切开，索引里只有 `verify`/`fts`/`a`，`MATCH 'verifyftsa'` 当然找不到。**用 `fts5vocab` 看过真实 token 表才确认**，没靠猜 |
| 09-17 | 2.6c **清理只能降级，不能删** | 清理污染库时选择 `status='archived'` 而不是 `DELETE`：archived 已经不进提示/约束（§6.5 红线），效果等价，但**可逆、可复查**。`tools/knowledge_audit.py` 强制这条（脚本里没有 delete 分支） |
| 09-17 | **`--selftest` 不指定 `--db` 会往默认库写假会话** | 默认库是**相对当前目录**的 `translations.db`，而自检要验落库往返、所以真的写会话。两个事实叠起来就是陷阱：从 exe 所在目录（`build\RelWithDebInfo\`）跑一次不带 `--db` 的 `--selftest`，**历史库里就多出几场 `engine=SelfTest` 的假会话**，它们会出现在"最近会话"里，将来还会被 agent 的 `list_sessions` / 交付物当成真会话读进去。我在项目根目录跑了几次，就留下一个只装着 11 场自检会话的 `translations.db`。**已修**：没显式给 `--db` 时自检自己开临时库（`%TEMP%`），跑完删掉；显式给了就照旧（那是"让新版程序打开某库一次以补触发器/迁移"的入口，必须保留） |
| 09-17 | **我给用户的命令少了一个参数，代价是他的库被写脏** | 我在交付说明里写 `--selftest`，文档里写的是 `--selftest --db t.db`。**从文档里抄命令，不要凭记忆重写** —— 少一个参数的后果不是"命令失败"，是**静默往用户的真实数据里写东西**。这次真正被写脏的只是一个新建的根目录 `translations.db`（历史库没被碰），但性质上不该靠运气 |
| 09-17 | **`--db` 是不是用户给的，本身就是个信号** | 修上面那个坑时发现：光有默认值不够，必须知道"用户到底指没指库"。于是 `AppConfig.db_path_explicit`。**别用一个值同时表达"默认"和"用户明确要求"** —— 需要区分时再加一个标志，而不是去猜路径长得像不像默认值 |
| 09-17 | **修复前的候选会在下次真跑时变成蠢问题** | 用户真实库里 11 条 candidate 全是修复前抽取器留的，虽然红线守住了（0 条 confirmed，从没进过提示词），但下次会话会问「已经听到 5 次「Froze」，它是对的说法吗？」。**"不影响行为"不等于"不用清理"** —— 提问是稀缺资源，一个蠢问题就在消耗用户对记忆的信任 |
| 09-17 | 2.6d **问用户一个他无法回答的问题** | 用户原话：「感觉根本不是人类的会问出的东西」。旧措辞「这个词的写法对吗？」把"拼写对不对"推给一个**刚听到、还没看到字**的人 —— 他在听音频，他不知道那个词该拼成什么样，只能瞎猜或跳过。**真正属于他的决定是"要不要让我把这个词记下来"**。更糟的是猜错会被记成约束（他按了 y，`Erika` 进了 `initial_prompt`）。**判据：问之前先问自己"用户凭什么知道这个答案"** |
| 09-17 | 2.6d **同一个实体被问成两个独立问题** | 库里 `Erica`（#43）和 `Erika`（#13）只差一个字母，旧代码当两条不相干的知识各问一遍。用户的两个回答**互相矛盾**，而他无从知道这两个写法指同一个人 —— 因为两个问题都没提到对方。**正确答案一直在我们自己手里**（两条只差一字母的记录就是"疑似同一实体"），把判断推给用户之前必须先把冲突摆出来 |
| 09-17 | 2.6d **`from == to` 被当成失败报给用户** | 写法冲突里用户选中**本来就是 confirmed** 的那个（最常见答案："现在用的对，另一个是听错的"）时，`set_status(id,"confirmed")` 失败 —— `can_promote` 里 `from == to` 返回 false（"状态没变就不是一次状态变更"）。于是用户答对了却看到 `[失败] 状态不允许从 confirmed 变为 confirmed`。**"无需变更"不是错误** |
| 09-17 | 2.6d **手写的用例计数会撒谎** | 缺口检测那组写着 `"✅ 11 例通过"` 是**硬编码**的，我加了 ⑫⑬⑭ 三条之后它照样打"11 例通过"。这跟"诊断工具撒谎"是同一类问题，只是更隐蔽（数字看起来像真的）。**计数必须跟着用例走，否则宁可不报数字** —— 失败时 `why` 会指名道姓，那才是有用信息 |
| 09-17 | 2.6d **全大写 ≠ 缩写** | 播客小标题 `PUTTING IT TOGETHER` 整行大写，8 字母的 `TOGETHER` 满足"连续 ≥2 大写"被当缩写抽出来。**真缩写都在 5 字母内**（CEO/KPI/OKR/SLA/SDK），而"被大写强调的普通词"任意长度 —— 长度就是分界。另外**整行大写是排版不是形状**，这种段落在"大写"上零信息量，应该整段作废而不是逐词猜 |
| 09-17 | 2.6d **合并错了比不合并更糟** | 写法冲突的合并条件卡得很紧（同首字母 + 长度 ≥4 + 编辑距离 ≤1）：判错的代价是让用户在两个**无关**词之间做选择。`PC`/`PB` 这类两字母词一律不合并，`Phoenix`/`Marco` 这种不相干的也必须各出一问（都有专门的自检用例） |
| 09-17 | 2.7 **系统"学了"和"懂了"是两件事** | 2.7 之前一条知识能表达的全部内容是"这个词该写成什么样"（key → value 的写法映射）。于是系统能做的只有"把 Erica 认成 Erica"，**永远做不到知道 CO-RE 是什么** —— 而用户能教的最有价值的东西恰恰是后者。加 `knowledge.definition` 之前，"越用越懂你"只有第一半 |
| 09-17 | 2.7 **`upsert` 会顺手改 `hits`，而"用户答话"不是"又听到一次"** | 用 upsert 写含义之后，`EnglishPod` 的 hits 从 3 变成 4 —— 上一场明明只听到 3 次，界面上写 4 次。**"给用户看的数字必须是真的"**这条在这个项目里已栽过两次（另一次是抽取器计数），所以为它单开一条 `set_definition()`，不碰任何其它字段 |
| 09-17 | 2.7 **默认长度上限会把最自然的回答判成跳过** | `interpret_answer` 默认 60 字节（专名够用），而「Compile Once – Run Everywhere，是我们 eBPF 项目的核心方案」就已经 60+ 字节 —— **用户教了，系统什么都没记住，而且看不出为什么**。长度上限必须能跟着问题的性质走。放宽不等于不设限，只是换个更合适的界 |
| 09-17 | 2.7 **老库加列比加表麻烦得多** | `CREATE TABLE IF NOT EXISTS` 只补表**永不补列**（已知）；而 `CREATE VIRTUAL TABLE IF NOT EXISTS` 和 `CREATE TRIGGER IF NOT EXISTS` 同样不会更新定义 —— 于是老库会落到最难看的状态：**内容表有 `definition` 列、FTS 索引里没有**，查询不报错，只是永远搜不到用户教它的那句话。做法：整块 DROP 重建 + 从内容表 `rebuild`（内容表一个字不动，索引是可重建的派生数据） |
| 09-17 | 2.7 **一问两用的问题，顺序错了就永远拿不到答案** | 「它指什么」原来是排在高频未确认**之后**的。但需求原话本来就是一问两用（"它是重要概念吗？如果是，它指什么？"）—— 排后面的话，hits=3 的术语先被问"我打算把它记成术语，对吗？"，用户按个 y 就结束，**确认拿到了、含义永远拿不到**。规则顺序不只是效率问题，它决定你能拿到哪种答案 |
| 09-17 | 2.8 **"文案难看"的背后是内部状态在漏** | 用户的感受是「我在给数据库做标注」。根因不是措辞：面向用户的中文**拼在 `KnowledgeGap.cpp` 里**，和判定逻辑同一个函数 —— 于是 `hits`（「已经听到 4 次」）、`confidence`（「识别置信度只有 0.40」）、`asked_count`（「最多再问一次」）、status 字面（「已确认：」）全顺着这条路漏了出去。**改掉措辞不解决问题，得把这条路切断** |
| 09-17 | 2.8 **解耦要靠结构，不能靠"注意别泄漏"的注释** | `Interaction.h/.cpp` 规定**不许 include** `KnowledgeGap.h` / `KnowledgeStore.h`，`Prompt` 里**刻意不放** hits/asked_count/confidence/status。理由：只要能拿到 `KnowledgeItem`，迟早有第一个文案顺手把 status 打出去。**让结构上拿不到**比写注释可靠 —— 而且它顺带带来两个好处：文案能单独自检（不需要数据库），将来 GUI/多语言能整层替换 |
| 09-17 | 2.8 **`hits` 是跨会话累加的，"这场听到 N 次"是假话** | `upsert` 里 `hits = hits + 本次次数`。实测 `erica` 的 hits=9 = #43 那场 3 次 + #46/#47 若干次。所以凡是说"这场会话里听到 N 次"的文案**都在说假话**，而且这个假话我自己写了不止一处。改用**定性**描述（`Familiarity` 三档：第一次 / 不止一次 / 多次）—— 跨会话语义下定性是真的，定量不是 |
| 09-17 | 2.8 **禁用词表这种笨办法真的有用** | 自检 ⑱ 列了一张禁用词表（`已经听到`/`置信度`/`candidate`/`已确认：`/`约束`/`知识库`…），把八种问法 × 七种回执全渲染一遍逐个查。**它当场抓住了我自己刚写的「不会再拿它当约束」** —— 「约束」是我们的内部词。文案类的问题靠人来 review 一定会漏（没人会去读自己的输出），只有机械检查兜得住 |
| 09-17 | 2.8 **改写循环时多套了一层下标** | `group` 里装的**就是** entries 的下标，我写成了 `entries[group[gi]]` —— 于是写法冲突的选项顺序被打乱，而且 `gi` 一大就越界。**自检抓住了**（报 `Erika(2) Erica(3)`，没按 hits 降序）。教训：给失败信息带上**实际值**（"实际顺序：…"）比只写"顺序不对"有用得多 —— 前者一眼看出是 double-index，后者只能回去读代码 |
| 09-17 | 2.8b **"先读旧值再写"不能省** | 把"读旧 definition"和"写历史"合成一条 `INSERT ... SELECT COALESCE(definition,'')`，而它跑在 UPDATE **之后** —— 历史里的 `old_value` 存的是**新值**，时间线看起来"含义从来没变过"。**不报错、不自检失败，只让"你凭什么这么说"这条承诺悄悄失效**。这类 bug 只有"人真的去看一眼历史表"才抓得到 |
| 09-17 | 2.8b **同一件事有两条路，就会走散** | `upsert` 里原本也有个 `write_definition`。2.8 把问含义改成不走 upsert（避免 hits+1）之后它就没调用者了 —— **直接删掉**，让"含义只能由用户提供、唯一入口 `set_definition()`"变成结构保证。留着它等于两条都能写含义的路 |
| 09-17 | 2.8b **`loop_evidence.py` 一眼看出污染事故** | 它把 `knowledge_history` 按时间线排出来，`t.db` 上直接打出 `user_confirmed: down / Keep / movies / Speaking / Midnight` —— 那一刻"系统在学垃圾"是**肉眼可见**的。**这类问题以前只能靠人自己翻日志发现**（实际就是等用户来问"这些词哪来的"）。可观测性不是加分项，它决定事故多久被发现 |
| 09-18 | **2.8e 用户答「是的」，系统把两个人名删了** | 「是的」不在肯定词表里（表里只有「是」「对的」「好的」），于是掉进"其余内容一律当用户给出的正确写法"分支 → `person marco = 是的` / `person erica = 是的`，**都是 confirmed** → `initial_prompt = "EnglishPod, CarsPacked, 是的, TV"`，而 **Marco / Erica 整个消失**。和 `preview = 1` 是同一类。**修法不是继续加词**（"是的/是啊/对呀/好啦"穷举不完），而是**剥掉句尾语气助词再查表** —— 核心词 + 助词的整类一起解决。**用户看到的是「好的，改成「是的」了。」，听起来还挺合理** —— 这类 bug 的可怕之处就在这 |
| 09-18 | **2.8d 真实启动打了 1483 行日志** | llama/whisper 加载阶段打几百行 control token / tensor / KV cache，把 `>>> 已开始记录 <<<` 埋在中间。用户往上翻一屏只看见满屏 tensor，**只能猜什么时候该放视频** —— 猜错几秒就意味着那几秒没录到。这不是美观问题，是可用性问题。装按级别过滤的回调（只放行 WARN/ERROR/CONT）→ 68 行。**保留 WARN 而不是只留 ERROR**：显存不足时先打一串 WARN 再失败 |
| 09-18 | **2.8f 复用一直在发生，但屏幕上一个字都没提** | 用户三场演示里 #1 和 #2 的 `initial_prompt` 一模一样 —— 复用确实发生了，但**没有任何一句话说出来**。复盘时只能靠事后工具翻库。**一个不说的机制，对使用者来说等于不存在。** 加了开工语：`我带着之前学到的 N 条知识` + 逐条列出（带用户教过的含义）。空库时也必须有一句"从零开始听" —— 否则用户分不清"没记忆功能"和"记忆是空的"，而后者正是"第一次不知道"的起点 |
| 09-18 | **"已经被用户确认"不等于"是对的"（第二次）** | 「是的」是用户**亲手打的**，系统照单全收成了约束。上一轮 8 个假阳性也全是用户逐条确认的。**上游该拦的不能推给用户** —— 这条一天之内验证了两次 |
| 09-18 | **`CarsPacked` —— 用户替系统圆了一个错** | 很可能是 ASR 把 "cars packed" 粘成了一个假专名（同场的 `WGBH` 是真的）。系统问"它是重要概念吗"，用户认真地给了定义「装车，将行李或物品装上车」。**演示时被追问"这真是专名吗"会不好答。** 已知限制：抽取器在英语播客上仍会把短语粘成假专名 |
| 09-18 | **演示只成功一半，缺的是最关键那一拍** | 三场真实会话证明了"它真的在学"（机制层），但**"第二次知道"看不见**、"第三次利用历史"没体现、**学到的东西和用户的工作无关**。最后一条是 `PRODUCT_LOOP.md` 早记下的结构性洞，不是 bug |
| 09-17 | 2.6b **中文会议原本"学不到任何知识"** | 抽取器的英文规则靠**首字母大写**，中文没有大小写 → 中文会议里「张伟负责下周的报价」**一个候选都抽不出来** → 知识库不长 → 没问题可问 → 三条腿没输入 → **"越用越懂你"在中文场景下完全不成立**。而中文会议正是这产品的主场。已补 R6（姓氏+佐证）/R7（后缀） |
| 09-17 | 2.6b **`strcmp` 用在前缀判断上** | 判断"人名后面跟的是不是动词"时，传进去的是**后面剩下的整串**（"负责下周的报价"），而我用了 `strcmp` **全等** → 只有动词正好落在串尾才匹配。后果：「张伟负责下周的报价」里的**张伟完全抽不到**，而「那边的**反馈**」里的假人名「边的」**反而被抽到**（反馈正好在串尾）。**批量用例才抓得出来，单个用例很可能刚好避开** |
| 09-17 | 2.6b **"人名里不能含虚词"要从第 2 个字查起** | 「那边**的**反馈」→ 边是姓氏、反馈是佐证动词 → 抽出假人名「边的」。但第 1 个字不能查：「于」本身是常见姓氏，一查就会把「于伟」这类真名字挡掉 |
| 09-17 | 2.6b **弱后缀要有出现次数门槛** | 「项目/系统/平台」太宽泛（"交付项目"），刻意**不收** 计划/方案/产品/版本/部门/团队/中心 —— 实测「交付计划」会被当成专名。报错的代价不是"多一条数据"，而是**用户被问一个蠢问题** |
| 09-17 | 2.6b **中文抽取没有真实数据可验** ⚠️ | 手写语料只能验"规则的形状"，验不了真实 ASR 输出上的准确率（同音错字、有时不加标点）。**需要用户录一场中文会议，再 `--extract <id>` 回来看**。这根 §8.8⑨ 是同一个约束，只是这次没有替代方案 |
| 09-17 | **环境坑：PowerShell 5.1 的 `>` 默认写 UTF-16LE** | 我按 UTF-8 读日志，看到"中文乱码"，**连续两次得出错误结论**（先说"命令行参数编码坏了"，更早还说"中译中模型拒答"）。日志必须按真实编码读 —— 用 Python 解 `utf-16` 或看开头有没有 `\xff\xfe` |
| 09-17 | **`--dump-prompt` 第三次和真实路径不一致** | 它无条件调 `translate_once`，**没有**主循环里那条"源==目标就跳过翻译"的保护。拿它测"中文会议"会看到误导结果（源==目标时模型做同语言改写：`let us start`→`let us begin`）。**真实链路**是：检测到 zh == 目标 zh → `passthrough` 落库（src=tgt），摘要那边 `tgt != src` 才重复发 → **中文会议本来就能出纪要** |
| 09-17 | **术语约束会凭空编造专名（回归，已修）** | 用户真跑会话 #44 发现：`and wife get ready to go` → 「埃丽卡和马可准备出发了」。**对照实验证实**是约束造成。修法：`ITranslator::glossary_for_text()` 只约束本段真出现过的术语 |
| 09-17 | **我造成的数据丢失事故（已修已恢复）** | `tools/ab_translation_constraint.py` 第一版直接 `DELETE FROM knowledge`，把用户 4 条 confirmed 全删了。已改成一次性 scratch 库 + 拒绝在有数据的库上运行 |
| 09-17 | 2.5b **诊断错了，是靠"数字对不上账"纠正的** | 净化器报 6 处坏字节、上游一行没报 → 模型回复是干净的 → 真病根是 `find_first_of(u8"：:")` 按单字节比较，把「的」（`E7 9A 84`）的 `0x9A` 当成了全角冒号 |
| 09-16 | 2.6 **闭环缺了一整个环节，是用户真跑才暴露的** | **设计文档里写了、执行步骤表里没有 = 不存在** |
| 09-16 | 2.6 **只修数据流一环 ≠ 修好** | 改了 `upsert` 采信 `hits`，重跑还是 1 —— 真相是 `save_candidates` 压根没传 `c.hits` |
| 09-16 | 2.4 **"提问稀缺"要有个地方记"问过了"** | `asked_count` + `kMaxAsks = 2`，**跳过也计数** |
| 09-16 | 2.3 **外部内容 FTS5 表必须用触发器维护** | 手动 `INSERT INTO knowledge_fts` 等于白写；`DELETE FROM knowledge_fts` 直接报 `database disk image is malformed` |
| 09-16 | 2.3 弱断言 = 假绿灯 | 旧自检只验"四张表在不在"，索引空的 bug 藏了两轮。改成"写进去→必须查回来→删掉→必须查不到"后**立刻抓出两个真 bug** |
| 09-16 | 2.2 **值变了必须把 confirmed 降回 candidate** | 原来那条确认是针对旧值的，值一变它就不成立（§6.5 要防的正是这个） |
| 09-16 | 2.1 迁移的边界 | `CREATE TABLE IF NOT EXISTS` 只补表、**永远不补列** —— 加列必须另走 `ALTER TABLE` |
| 09-19 | **验了逻辑 ≠ 验了落点（第四次）** | 判断缓存的自检 ⑩ 用**内存 map** 验编排：命中不联网、失败不落缓存、Ask 不落缓存 —— **全绿**。但缓存真正的实现是 SQLite 那张表，它一行都没被碰过。把 INSERT 的表名故意改错，⑩ **照样通过**，只有新加的 ⑫（跑真实 TriageCache）报「走了真实编排，但 triage_verdicts 里一行都没有」。**"调用了一个回调"和"那一行真的写进表了"之间可以整段是坏的**（SQL 拼错、表名错、绑定错）。本项目第 2.3 节栽过同一个坑（自检只验"四张表在不在"），这次是主动堵的 |
| 09-19 | **`friend class X` 在命名空间里必须写全限定名** | `friend class TriageCache;` 声明的是全局的 `::TriageCache`，而真正的类在 `triage::` 里 → 编译期一路 `C2248 无法访问 private 成员`，报错完全看不出是 friend 写错了命名空间。另外 **friend 只授予那个类的成员函数**：我写的匿名 namespace 里的 `db_of()` 自由函数拿不到私有 `db_`，想"帮忙拿连接"就得把连接暴露给一个谁都能调的名字 —— 那正是 friend 要避免的。改成每个成员函数各自加锁取连接 |
| 09-19 | **同一实体被问两遍（真 bug，端到端验出来的）** | 表的身份是 `(kind, key)`，而 **kind 是可修正的属性**：抽取器只会说"首字母大写的词 → term"，分诊层的模型看完那句话会把它改成 product（`set_kind`）。改完之后下一次抽取同一个词，`(kind,key)` 就对不上了 → upsert 找不到旧行 → **又建一行**。实测：`Nimbus` 在一场里被问了**两次**，库里 `(product,nimbus)` + `(term,nimbus)` 两行，`kMaxAsks` 各算各的（上限被悄悄翻倍）。**真实库里 `Erika` 的 person/term 两行就是这个原因 —— 我之前猜"两场会话各贡献一个 kind"，猜错了。** 修法：写入前 `find_by_key()` 认旧行并**沿用库里的 kind**（它是更知情的那个）；旧库靠 `detect_gaps()` 按 key 去重兜住（去重的是**提问**不是数据，§6.5 不许悄悄删记录） |
| 09-19 | **测试数据复用同一个值 = 测的是别的东西（第四次）** | 缺口检测"上限截断到 5"那条用例造了 20 条 value 全为 `"T"` 的记录 —— 那是**同一个实体的 20 份拷贝**，去重一上来就并成 1 条，用例当场失败。不是去重太狠，是**用例本身没意义**（库里不可能有 20 个都叫 T 的实体）。`mk()` 这类测试数据踩坑这是第四次（前三次见 §一 的 ㉑ 组注释）。**造用例之前先问：这个数据在真实库里可能出现吗** |
| 09-19 | **失败信息被后面的用例盖掉，症状指错方向** | 自检的 `twhy` 是**覆盖式**的：故意把"判断失败写进缓存"（严重：会静默让某个词永远不被问），报出来的却是后面用例的「要问被写进了缓存」（轻微）。**症状指错方向比不报还费时间** —— 你会去修一个没坏的东西。安全关键的那几条改成"第一条失败锁定"，打印时优先 |
| 09-19 | **临时破坏验证：每次新增自检都要先证明它会失败** | 今天对 4 条新断言各做了一次故意破坏（去掉 Skip 条件 / 把失败也存进缓存 / 改错 SQL 表名 / 关掉 find_by_key），确认自检**每次都能指名道姓报出来**，然后才恢复。**没做过这一步的断言就是"永远绿的摆设"** —— 它和注释里的承诺没有区别 |
| 09-19 | **诊断工具撒谎（第四次）：`loop_evidence.py` 自己拼 `initial_prompt`** | 它用 SQL 自己拼"下一场会有哪些词"，**漏了去重**，打出 `Marco、EnglishPod、Erika、Erika、VAD`；真实路径 `--terms` 只给一个 `Erika`。讽刺的是这个文件的**开头就写着**"抽取那一步刻意调同一个 exe，重写一份就是两个实现，两边迟早走散" —— 它违反了它自己写的原则。撒谎方向最坏：**让好库看起来像坏库**。已改成直接调 `--terms`，并加了**交叉核对** |
| 09-19 | **"验了逻辑"≠"验了落点"（第四次）** | 判断缓存的自检用**内存 map** 验编排 → 全绿；但缓存真正的实现是 SQLite 那张表，一行都没被碰过。把 INSERT 的表名故意改错，内存版**照样通过**，只有跑真实 `TriageCache` 的那组报错。**"调用了一个回调"和"那一行真的写进表了"之间可以整段是坏的**（SQL 拼错、表名错、绑定错） |
| 09-19 | **"带出处"不配核对，比不带出处更危险** | 模型编的 `#9002·7`（那一场只有 4 段）和真的 `#9002·3` 在屏幕上完全一样。所以 5.7 做的一半是"打印出处"，另一半是**证伪**。`--verify-report` 里**零引用报 exit 2 而不是 0** —— 一份没有出处的报告是"没做这件事"，不是"做对了"；算它通过等于给最该被质疑的报告发绿灯 |
| 09-19 | **演示脚本的断言要钉性质，不要钉措辞** | `demo.py` 第 2 幕原来断言"必须出现『项目中的一个重要概念』"，**配上 API Key 之后就失败了** —— 因为模型把「极光平台」判成了 `product`，问法换成「这是你们的产品吗」。**那是更对的行为，却被断言判成错**。改成断言"问法按类型变了"之后，那一幕反而成了亮点（`term → product`） |
| 09-19 | **一个字段需要模型替我们道歉，那就该修** | agent 生成的周报里主动写「行动项里的"负责人"字段有抽取错误（把"了，张伟""测报告，"当成了人名），**原始记录里的字段是脏的**」。旧实现是"从 负责/跟进 往前数 4 个汉字"，不判标点、不懂修饰语、更不知道什么叫人名。修法是**公开一个谓词**（`looks_like_person_name`，和抽取器 R6 共用同一套姓氏表/常用词表），而不是再写一套。**宁可留空，也不写错人** —— 那一栏是评审照着去找人的 |
| 09-19 | **种子数据必须遵守管道的真实约定** | 演示种子里我把 `tgt_text` 填成占位串 "(中文会议，无需翻译)"，而中文会议的约定是 **passthrough（tgt = src）**、且行动项抽取读的是 `tgt_text` —— 于是抽取器**把我那句占位符当成了任务文本**，校验器报「没有可执行的动作词：(中文会议，无需翻译)」。**我在验自己的占位符，而不是在验产品。** |
| 09-19 | **"破坏性验证"本身也会失效** | 验一个断言有没有用时，我把破坏写成了 `if (false && …) { continue; }` 再补一个做同样事的 `else` —— **等价于没改**，自检照样全绿，我差点得出"自检没覆盖到"的错误结论。**改完之后必须确认它真的改变了行为**（这次是靠"本该变红却没变红"发现的） |
| 09-19 | **"验证过了"和"验证了它有用"是两件事** | 识别腿从 2.5 到现在，验收就是 `--terms` 打印出那串 `initial_prompt` —— **那只证明词被拼出来了**。补了 A/B（每档重复 3 次，结果一致）之后才发现：喂**与音频无关**的真实词表，Whisper **吞掉了 "ask what you"**（114→101 字符）；喂 4 个编造词，它**幻觉**出 "Thank you for watching!"；喂到 40 个（原上限），整句 "Thank you" 不见了。而知识库是**跨会议全局累积**的，"和本次内容无关"恰恰是常态。**收益未证实 + 代价已证实 → 关掉那条路**，收益改走确定性的 `TermFixer`（拿到全量词表，不会幻觉） |
| 09-19 | **A/B 必须重复跑，否则会把噪声报成效应** | 第一次只跑了 1 次/档，看到转录不同就想下结论。加上 `--repeat 3` 之后才确认"每档 3 次完全一致" —— **这个确认本身才是结论成立的前提**。如果某档档内就不稳定，它和基线的差异根本不能算 prompt 造成的 |
| 09-19 | **文档说"已通"而实际关着，比没有文档更坏** | 改了识别腿的默认之后，`PROJECT.md` §6 的三腿现状表还写着「→ Whisper initial_prompt ✅ 已通」，`demo.py` 第 3 幕的旁白还说"出现在 ① 识别提示 里"。**两处都在说假话**，而且是最容易被信的那种（有表格、有 ✅）。改完代码必须同一轮把"说这件事的所有地方"翻一遍 |
| 09-19 | **分支指针和推送要一起做，别分两次** | 一次 `git push` 报「Everything up-to-date」而远端其实没动 —— 因为被取消的那条命令里**同时**含 `git branch -f` 和 push，我重试时只抄了 push，本地分支还指着旧提交。教训：**不要相信工具说的"没事发生"，去问事实**（`git ls-remote`）。这跟当天修的"诊断工具撒谎"是同一类问题 |
| 09-19 | **`Set-Content -Encoding UTF8` 会把整个文件搞成双重编码** | PowerShell 5.1 的 `Get-Content -Raw` 按 **ANSI** 读、`Set-Content` 按 UTF-8 写 → `SpeechEngine.cpp` 全文中文变乱码、还吃掉一个引号，编译直接爆。用 `git checkout` 恢复后**只用 edit 工具**重做。⚠️ 这个坑 `STATE.md` §五**早就记着**（"`>` 默认写 UTF-16LE"），我只记住了一半 —— **同一个根因换个命令又踩一次** |
| 09-19 | **自检用例自己构造错了**（写"不该发生"的用例时） | 语言策略第 ④ 条我写成 `zh,en,zh,zh,zh` 却期望"不切换"，而打断之后确实又有连续 3 段 zh —— **切换是对的**，是用例错。写"不该发生"的用例必须确认序列里**真的**不满足触发条件 |
| 09-19 | **"多检测几段更稳妥"是错的：`auto` 本身在损害识别** | 我原以为"首次锁定也要求连续一致"更稳（防开场音乐带偏）。实测把它否掉了 —— jfk 上 `--lang en` 固定 / 旧行为 / 让前 3 段走 auto，分别是 **106 / 79 / 8 字符**。原因是 `auto` 要在那 3~4 秒里**同时判语言和转写**。结论：**锁定要快、切换要慢**（判错的代价由切换机制兜住，延迟锁定的代价没有兜底） |
| 09-19 | **诊断工具撒谎（第五次）：`--dump-prompt` 只测本地混元** | 它函数里直接构造 `HunyuanTranslator`，**不看 `--translator`**。我加了 `--translator cloud` 去"验云端上下文"，跑出来耗时 177ms（本地量级）、输出和本地一模一样，才发现测的还是本地。**要验云端得单独写 harness。** |
| 09-19 | **harness 的默认值会悄悄过期** | 「知识库不进 initial_prompt」改成默认关之后，`asr_prompt_ab.py` 没跟着加 `--asr-prompt-kb`，于是**两档实际都是"无提示"**，跑出一模一样的结果。我差点把它当成"prompt 无害"的证据。已修，并加了一行**断言式提示**：造了 N 条知识而 prompt 为空时明确打"对照不成立"。 |
| 09-19 | **诊断工具撒谎（第六次）：把测试沙箱的故障当成产品缺陷** | `--ui` 报 `CreateCoreWebView2Controller hr=0x8000FFFF E_UNEXPECTED`，我立刻断定是"没初始化 COM 套间"，加了 STA 并**在注释里写下"不做的后果实测过"**。后来发现：**沙箱里这个错必然出现**（窗口、环境、`msedgewebview2` 子进程、用户数据目录全都正常，只有最后一步失败），**沙箱外同一个二进制完全正常**。于是那句"实测过"是**假的** —— 我把自己没有验证过的因果写成了结论。补做了真实验（MTA/STA/不初始化三种组合）之后才把注释改写成只有事实的版本。**教训**：①先说清"这是在什么环境里失败"再下结论；②**注释里的"实测过"三个字是承诺，写之前必须真的做过**。 |
| 09-19 | **"成功了"和"没失败"不是一回事（`start()` 的漏洞）** | `WebViewWindow::start()` 等 8 秒后**只判 `failed`**：既不成功也没报错 → **返回 true**。而 `--ui-verify` 接着会调 `wait()`，看门狗定时器却是在控制器回调里才装的（回调压根没跑过）→ **永久挂住**。首启动本来就慢，8 秒并非不可能超。修法：超时也算失败并说清原因，上限放宽到 20 秒。**排查要点**：写"等待某条件"的循环时，**超时分支必须和失败分支一样明确**，否则"等不到"会伪装成"成功了"。 |
| 09-19 | **看门狗只装在成功路径上 = 没装** | 同一个 bug 的另一半：`SetTimer` 写在控制器回调里。回调本身没跑起来时定时器就不存在，于是"保险丝"只保护了**本来就不需要保险**的情况。**兜底机制的挂载点必须不依赖它要保护的那条路径成功。** |
| 09-19 | **错误信息用赋值 = 后面的原因会吃掉前面的** | WebView2 的失败链上，环境回调**覆盖**了套间初始化写下的 `im->error`，我看到的就只剩最后一个错误码 —— 而真正的线索是第一个。改成追加（用竖线连接）。同一个位置还有第二处撒谎：HRESULT 打的是十进制 `-2147418113`，**这个数字在网上搜不到任何东西**，换成 `0x8000FFFF (E_UNEXPECTED)` 才可查。 |

---

## 五、这台机器的环境坑

完整清单见 `PROJECT.md` §4.5，这里是**最常撞到的四条**：

| 坑 | 解法 |
|---|---|
| `git push` / `ls-remote` 报 `schannel: SEC_E_NO_CREDENTIALS` | 加 `-c http.sslBackend=openssl`。**`ls-remote` 匿名可读，所以容易误判成"远程没问题"** |
| `git push` 报 `sh.exe: couldn't create signal pipe` + 读不到用户名 | 沙箱禁命名管道 → 凭据助手起不来。**需要在更宽权限下跑，或由用户在普通终端推** |
| 构建报 `LNK1168`（exe 被占用） | 先 `Get-Process Translator \| Stop-Process -Force` |
| PowerShell 读中文文件显示乱码（`绾跨▼瀹夊叏`） | **误报**：文件是合法 UTF-8，是 `Get-Content` 用 GBK 解码显示。**别据此改编码**，用字节级校验 |
| `Select-String` 默认**不区分大小写** | 扫平台 API 时 `WPARAM` 会撞上 `wparams`，产生假命中（被骗过一次） |
| 改了 `CMakeLists.txt` 的编译定义后只 build | **必须重新 configure**，否则不生效（加 `SQLITE_ENABLE_FTS5` 时踩过）。**新增源文件也要重新 configure**（`ModelLog.cpp` / `Interaction.cpp` 都踩过） |
| **`git commit -m "多行"` 会被 PowerShell 弄坏** | 第二段被当成路径（`did not match any file(s) known to git`）。**一律写进文件用 `git commit -F <文件>`**，别用多个 `-m` |
| PowerShell 里 `python -c "…"` 带中文/引号必然坏 | 引号被吃掉、中文变 GBK。**写成 `.py` 文件再跑**。`Set-Content`/`Out-File` 记得显式 `-Encoding utf8` |
| 往管道里喂中文（`$ans \| Translator.exe --ask`） | 要先 `[Console]::OutputEncoding = [Text.Encoding]::UTF8` + `$OutputEncoding = [Text.Encoding]::UTF8`，否则中文参数/输入变成乱码 |
| **`--db t.db` 是相对当前目录的**，而 `build\RelWithDebInfo\` 和仓库根**各有一个 `t.db`** | 两个不同的文件。验证/排查前先 `pwd`。`verify_memory.py` 现在会打**绝对路径**，一眼看出验的是哪个（这个坑真实发生过：验证报"缺少记忆表"，其实只是验错了文件） |

此外几条常撞到的：**仓库里有 8 个 `.db` 文件**（`build\RelWithDebInfo\` 下有 t.db / translations.db / sum.db /
t6.db / test_s4.db / test_step1.db，根目录还有 t.db），而 `--db` 是相对路径 ——
**真实会话历史在 `build\RelWithDebInfo\translations.db`，不在 `t.db`**（2.5 找"会话 #11 案例"时在这上面绕了一圈）。
引用"真实案例"时务必写清是哪个库。**`_` 在 SQL `LIKE` 里是单字符通配符**（`purge_key_prefix` 用的就是 `LIKE`，
所以 `__selftest_` 实际匹配 "任意两字符 + selftest + 任意一字符"，隔离性比看上去弱 ——
目前够用，但别把前缀隔离当成安全边界）；**FTS5 外部内容表不能用普通 `DELETE`/`INSERT`**
（报 `database disk image is malformed` 或静默不生效，只能用触发器或 `'rebuild'`）；
**MATCH 左侧不认表别名**；**PowerShell 5.1 的 `>` 默认写 UTF-16LE**（按 UTF-8 读日志会看到“中文乱码”，我因此连错两次结论）；**PowerShell 5.1 把无 BOM 的 `.ps1` 当 GBK 读**（脚本里的中文串会被破坏，
所以驱动脚本用 Python 写，不用 .ps1）。

---

## 六、收工前自检

```powershell
# 0. 先杀进程，否则链接失败 LNK1168
Get-Process Translator -ErrorAction SilentlyContinue | Stop-Process -Force

cd C:\dev\projects\AudioTranslator

# 1. 构建
cmd /c "call ""C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"" >nul 2>&1 && set VCPKG_ROOT=C:\dev\vcpkg && cmake --build build --config RelWithDebInfo --target Translator"

# 2. L1 自检：必须 **35 组**全过（数是"带 ✅/❌ 的行数减 1"，最后那行是总结）
#    ⚠️ 不给 --db 时自检自己开 %TEMP% 下的一次性库并跑完删掉（不会碰你的 translations.db）。
#       要验**指定的库**（或让它补触发器/迁移）就显式加 --db。
.\build\RelWithDebInfo\Translator.exe --selftest
.\build\RelWithDebInfo\Translator.exe --selftest --db build\RelWithDebInfo\t.db
#     2.8 之后新增的组：交互层文案（类型化提问/理由/回执/内部状态不外泄）、
#     选择题解析、背景去重、真实数据抽取回归、缺口检测 ⑫–⑲

# 2a. **长期记忆有没有真的生效**（2.6c 起）—— 不加载模型，秒级
.\build\RelWithDebInfo\Translator.exe --terms --db build\RelWithDebInfo\t.db
#     期望：① 那串 initial_prompt **只含真专名**，一个普通词都不许有
#           ② 条数 = confirmed 里 name-like 的去重数（同 key 不同 kind 只算一次）
#           ③ 摘要背景带 `（术语）：<含义>`
#     ⚠️ 这是唯一能低成本看见 §6.5 红线可观测面的入口。库一脏这里立刻现形：
#        曾经打出 `Erica, down, Keep, EnglishPod, movies, TV, Marco, ..., 1`
#        也曾打出 `EnglishPod, CarsPacked, 是的, TV`（用户答「是的」那起事故）
#     真实会话启动时还会打一句**开工语**（2.8f）：
#        [复用] 开工之前先说一句：我带着之前学到的 N 条知识。· X —— <含义> …
#        空库时是「我这边还没有关于你的任何记录，这一场从零开始听。」

# 2a2. 库的卫生审计（2.6c 起）—— 独立 sqlite3 oracle，列出可疑条目
python tools\knowledge_audit.py --db build\RelWithDebInfo\t.db
#     清理只能用**降级**（脚本里没有 delete 分支）：
python tools\knowledge_audit.py --db build\RelWithDebInfo\t.db --archive <key>... # 先 dry-run
python tools\knowledge_audit.py --db build\RelWithDebInfo\t.db --archive <key>... --apply

# 2a3. **跨会话闭环的证据**（2.8b 起）—— 演示和答辩就靠它
python tools\loop_evidence.py --db demo.db
#     三列：① 这一场抽出什么候选 ② 这一场之后库里变了什么（时间戳，精确）
#           ③ 到这一场结束时，下次开会能带上哪些词（**近似重建**，工具自己标注了）
#     ⚠️ ② 为空 = 那一场没让系统学到东西（用户没回答问题，或没有候选）

# 2b. 知识库数据层（第 2 阶段每步都要跑）——独立实现验证，且不改动原库
#     ⚠️ 一定要用**程序真正在用的那个库**：--db 是相对当前目录的
python tools\verify_memory.py build\RelWithDebInfo\t.db

# 2c. 缺口检测端到端（2.3 起）：seed → 应问 3 个 → clear → 应问 0 个
python tools\verify_memory.py build\RelWithDebInfo\t.db --seed-demo
.\build\RelWithDebInfo\Translator.exe --gaps --db build\RelWithDebInfo\t.db
python tools\verify_memory.py build\RelWithDebInfo\t.db --clear
.\build\RelWithDebInfo\Translator.exe --gaps --db build\RelWithDebInfo\t.db

# 2d. 确认交互端到端（2.4 起）：管道喂回答，走**真实循环 + 真实落库**
python tools\verify_memory.py build\RelWithDebInfo\t.db --seed-demo
"y`nn`nMarco" | .\build\RelWithDebInfo\Translator.exe --wav C:\dev\projects\whisper.cpp\samples\jfk.wav --summarizer rules --ask --db build\RelWithDebInfo\t.db --out v_out
#    期望：已确认：Qwen ASR / 已记下这个写法不对 / 记住了：Marco / 记下 3 条，跳过 0 条
.\build\RelWithDebInfo\Translator.exe --gaps --db build\RelWithDebInfo\t.db
#    期望：只剩 1 个（Phoenix，最后一次机会）—— confirmed 的不再问
python tools\verify_memory.py build\RelWithDebInfo\t.db --clear

# 2e. 自动抽取 + 整条闭环（2.6 起）—— **拿真实会话补学，不用重录音频**
#     先用只读模式看抽取器认出了什么（不会改库）
.\build\RelWithDebInfo\Translator.exe --extract 43 --db build\RelWithDebInfo\t.db
#     期望：EnglishPod 播客那种素材应抽出 Marco / Erica / EnglishPod；
#           jfk.wav 那种"每句首词大写但没专名"的应抽出 **0 个**
#     再真的走一遍闭环（抽取 → 落候选 → 问 → 确认）
"y`ny`ny`ny" | .\build\RelWithDebInfo\Translator.exe --extract 43 --apply --ask --db build\RelWithDebInfo\t.db
#     期望：[Extract] 已写入 4 条候选（全部 status=candidate）
#           [确认] 第一次听到「Marco」… → 已确认
#     然后拿原来那句失败译文验约束生效：
.\build\RelWithDebInfo\Translator.exe --dump-prompt "How are you, Erica? Marco, I'm doing really well." --db build\RelWithDebInfo\t.db
#     期望：输出 Erica，你怎么样？Marco，我过得很好。（原来会被音译成 埃里卡/马可）

# 3. 端到端（可选，约 20 秒，零交互）
.\build\RelWithDebInfo\Translator.exe --wav C:\dev\projects\whisper.cpp\samples\jfk.wav --summarizer rules --db v.db --out v_out

# 3b. §6.5 红线的**行为级**回归（2.5 起）：真句子 + 真模型，约 40 秒
#     断言：① 基线逐字复现历史失败 ② candidate 与基线逐字相同 ③ confirmed 纠正译文
python tools\ab_translation_constraint.py
#     跑完会自己清空 knowledge 表

# 3c. Agent 工具层（5.1/5.4 起）：注册、schema、真跑、出错路径、限长 —— 不联网
.\build\RelWithDebInfo\Translator.exe --tools
python tools\agent_tools_check.py
#     期望：[Tools] 已注册 6 个工具（5 个只读 + 1 个「提议」）

# 3d. 判断缓存（2.12 起）—— 不加载模型，几秒
.\build\RelWithDebInfo\Translator.exe --triage-cache --db build\RelWithDebInfo\translations.db
#     期望：末尾有 [triage-cache] total=N reused=M，且红线核对说"一个都没进约束"
#     纠错出口（判错的词从此不再被问时用）：
#       --triage-cache --forget <词>    /    --triage-cache --clear

# 3e. 闭环证据（2.8b 起）—— **只读**，可安全跑在真实库上
python tools\loop_evidence.py --db build\RelWithDebInfo\translations.db --last 3
#     期望：末尾 ③b 是真实路径（调 --terms），且有「✅ 交叉核对：近似重建 == 真实路径」

# 3f. 缓存/分诊的**真会话路径**验证（不用录音、不等 Whisper）
#     造一场内容可控的会话（文本是照抽取器规矩写的，见脚本注释）
python tools\seed_triage_session.py build\RelWithDebInfo\tc.db
$env:DEEPSEEK_API_KEY='...'      # 只走环境变量，**绝不写进任何文件**
"n`nn`nn`nn`nn" | .\build\RelWithDebInfo\Translator.exe --extract 1 --apply --ask --db build\RelWithDebInfo\tc.db --out out_tc
#     第一次期望：[分诊] … （model），末尾「挡掉 N」
"n`nn`nn`nn`nn" | .\build\RelWithDebInfo\Translator.exe --extract 1 --apply --ask --db build\RelWithDebInfo\tc.db --out out_tc
#     第二次期望：同样的词变成 （cache），末尾「其中 N 个走缓存（未联网）」
#     ⚠️ 用一次性的 tc.db，跑完删掉；不要在真实库上做

# 3g. 行动项（5.5）：三场演示种子会 → 跨会话聚合
python tools\seed_demo_week.py build\RelWithDebInfo\tact.db
foreach ($s in 9001,9002,9003) {
  .\build\RelWithDebInfo\Translator.exe --export $s --summarizer rules --db build\RelWithDebInfo\tact.db --out out_act
}
.\build\RelWithDebInfo\Translator.exe --actions --db build\RelWithDebInfo\tact.db
#     期望：「接口文档」那条带「（在 2 场会话里被提到，最近 #9002）」；
#           负责人应是「张伟」而不是「了，张伟」
#     幂等：同一场再 --export 一次 → 「新建 0 条…本轮新增关联 0 条」
#     状态：--actions --done <id> → 再 --export 同一场，**不许**打回 todo、不许新增行

# 3h. 出处核对（5.7）
.\build\RelWithDebInfo\Translator.exe --verify-report out_act\session-9002\meeting-9002.md --db build\RelWithDebInfo\tact.db
#     期望：citations=N ok=N bad=0，exit 0
python tools\verify_report_check.py
#     期望：三份样本分别 exit 0 / 1 / 2（全真 / 混编造 / 零引用）

# 3i. 一条命令演完全部能力（5.8）—— **答辩时就跑这个**
python tools\demo.py
#     六幕；不需要 API Key（设了就多演 agent 周报那一幕）
#     不加载 ASR 模型、不碰声卡、0 秒冷启动
#     ⚠️ 第 6 幕读真实库但**只读**

# 4. 提交
git add -- <明确列出的文件>
git status --short          # 确认没有多余文件
git commit -F <写好的提交信息文件>

# 5. 推送（不触碰 main）
git branch -f assistant-baseline HEAD
git -c http.sslBackend=openssl push origin assistant-baseline
#     ⚠️ 必须带 http.sslBackend=openssl：schannel 在本机报
#        AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS
```

**然后回答三个问题，答不上就是没做完**：

1. 这一步跑到哪一级验证了？没跑到的级别，如实标注 ⚠️
2. **本文件（`STATE.md`）更新了吗？**
3. `PROJECT.md` 该同步的同步了吗？（规则见它 §8.5）
